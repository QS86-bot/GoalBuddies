# Engineer Review — agenda voor november

Alles waar ik (Quinten) of de agents niet 100% zeker over zijn.
Aanvullen tijdens het bouwen, niet achteraf reconstrueren.

## Hoe je deze lijst leest

**Een doorgestreept risico (`~~Hoog~~ opgelost`) is afgehandeld; al het andere
is open werk.** `npm run review:controle` bewaakt dat die kolom klopt en draait
mee in `/audit`.

⚠️ **Een rij beschrijft de stand op de dag van schrijven, niet die van vandaag.**
Op 25-08-2026 bleken drie rijen die als **Hoog** of **Middel** open stonden al
lang gerepareerd — `is_group_member` had zijn `status`-toets, `weekly_goals_select`
schermde `excused` al af sinds 0047, en `join_group_with_code` had alle drie de
limieten die de rij "nog open" noemde. Dat kost meer dan een rommelig document:
**een lijst waarvan een deel al klaar is, kost de lezer het vertrouwen in de
rest.**

⚠️ **Meet daarom de gedeployde stand voordat je een rij gelooft** — `pg_get_functiondef()`
en `pg_policy`, niet het migratiebestand en niet deze tekst. Dat is dezelfde
regel als bij onwrikbare regel 19: *"verifieer elke bevinding zelf voordat je
hem verwerkt."*

⚠️ **En schrijf een reparatie in de rij zélf op, met een ✅ en het migratienummer.**
Alleen dan kan de controle zien dat hij klaar is; "gedeeltelijk opgelost" of
"kan opgelost worden door" telt bewust niet mee.

### De twee stempels

| Stempel | Betekenis |
|---|---|
| ✅ | Gerepareerd. De risicokolom is doorgestreept en de rij is klaar. |
| 📏 | **Nagemeten op de genoemde datum en nog steeds open.** Niet opgelost, wél geverifieerd tegen de gedeployde stand. |
| 🗣 | **Niet te meten.** Een ontwerpvraag, een heuristiek zonder ijkpunt, of iets dat pas uit gebruik blijkt. Dit is agenda voor het gesprek, geen achterstallig onderhoud. |

⚠️ **Een rij zonder stempel is nooit tegen de werkelijkheid gehouden** — hij zegt
alleen wat er op de dag van schrijven werd gedacht. Dat is geen diskwalificatie,
maar het is wel iets anders dan een gemeten bevinding.

**Stand op 25-08-2026:** elke open rij boven niveau Laag draagt een stempel. De
Laag-rijen dragen in plaats daarvan hun eigen `**Wordt zwaarder als:** …`, wat
dezelfde functie heeft: het zegt wanneer je opnieuw moet kijken.

⚠️ **En een grep is geen meting.** Bij `invite_preview` meldde een regex op
`invite_events|count(*)` dat er een limiet stond; die `count(*)` telde de léden.
Lees de functie, niet een patroon erin.

| Datum | Bestand / module | Waar ik onzeker over ben | Risico |
|---|---|---|---|
| 2026-08-23 | Servicewormer-scope tegenover `RewriteBase /goalbuddies/` | `public/manifest.json` gebruikt `start_url: "/"` en `scope: "/"`, `app/+html.tsx` linkt `/manifest.json`, en QS8-124 registreert `/sw.js`. `docs/DEPLOY.md` beschrijft daarentegen een `.htaccess` met `RewriteBase /goalbuddies/`. Draait de app onder een pad in plaats van op een eigen (sub)domein, dan staat de worker op `/goalbuddies/sw.js`, bedient hij alleen dat pad, en klopt geen van de absolute verwijzingen meer. **Niet gerepareerd:** welke van de twee waar is, hangt af van hoe het subdomein bij Hostinger is ingericht, en dat kan ik hier niet zien. Het breekt bovendien niets zichtbaars behalve meldingen, dus het is precies het soort verschil dat pas opvalt als iemand klaagt dat hij niets krijgt. Vraag voor de review: hoort een deploy een controle te draaien die `/sw.js` en `/manifest.json` opvraagt en op status én content-type toetst?  📏 **Nagemeten op 25-08-2026 en staat nog.** `public/manifest.json` staat nog op `start_url: "/"` en `scope: "/"`, terwijl de app onder `/goalbuddies/` draait. | Middel |
| 2026-08-23 | Ontkoppelen als omzeilprimitief — **tweede keer, nu met de score eraan** | ✅ **Gedicht in 0066.** `kan_beoordeeld_worden()` (0064) beantwoordde "kon iemand deze week beoordelen" op het moment dat het punt geboekt werd, door te kijken of het doel op dát moment aan een groep hing. De eigenaar mag `goal_group_links` onvoorwaardelijk verwijderen én terugzetten, dus: ontkoppel op vrijdag, laat de rollover langsgaan, koppel maandag terug — geen minpunt, elke slechte week, score kan alleen nog omhoog. Domeinregel 10 verbiedt dat met zoveel woorden. **Waarom dit hier staat en niet alleen in het beslisdocument:** exact hetzelfde primitief staat al in de rij van 17-08 ("Bewijseis te omzeilen met ontkoppelen"), en is toen bewust als *Laag* weggelegd omdat het zelfbedrog was en geen autorisatiegrens. Dat oordeel klopte toen. Vier dagen later is er een feature bovenop gebouwd die er wél een scoregat van maakt. ⚠️ **De vraag voor de review: hoe merk je dat een als 'Laag' weggelegd primitief zwaarder wordt door iets dat je er later op bouwt?** Dit dossier had het antwoord al — de A17-aantekening ("herbevestigen vóór EPIC 12") werkte precies zo — maar er stond geen zo'n aantekening bij deze rij. Reparatie is een grendel op `weekly_goals.beoordeelbaar` die maar één kant op beweegt; zie `docs/decisions/2026-08-23-de-grendel-op-het-minpunt.md`. | ~~Hoog~~ opgelost |
| 2026-08-23 | Een CHECK op een kolom die de enige schrijver niet vult | ✅ **Gedicht in 0067.** 0062 zette `check ((platform='web') = (p256dh is not null and auth is not null))` op `push_tokens` en wijzigde `registreer_push_token()` niet mee — die functie kende de sleutels niet en schreef NULL. Elke webregistratie liep dus op een CHECK-schending stuk, ongevangen, dus de gebruiker kreeg een ruwe 23514 door PostgREST heen in plaats van `{ok:false, reason}`. De RPC is aan `authenticated` gegrant, dus elke ingelogde gebruiker kon die 500 opwekken. **De tabel was leeg, dus de migratie slaagde en er ging niets zichtbaar stuk.** Web push was dood zodra hij aangezet werd, en dat was pas gebleken bij QS8-114. Vraag voor de review: hoort er een controle in het migratieproces die bij een nieuwe constraint de schrijvers van die kolom opzoekt? | ~~Middel~~ opgelost |
| 2026-08-23 | `z.string().url()` is geen schema-allowlist | ✅ **Gedicht in 0068** (CHECK) **en in het schema** (`.refine()` op `https://`). Nagemeten met zod 4.4.3: `javascript:alert(1)`, `data:text/html,…` en `file:///etc/passwd` zijn alle drie geldig volgens `.url()`. `commitments.image_url` had server-side nul validatie. Domeinregel 11 geeft de begunstigde groep leesrecht zodra de straf verschuldigd wordt, dus zodra iets dat veld als link of `<img>` rendert is dit opgeslagen XSS richting groepsgenoten. Vandaag onbereikbaar (geen renderpad). ⚠️ **Wat dit erger maakte:** de nieuwe test in dezelfde PR legde de te ruime regel vast als correct gedrag. Een test die een gat bekrachtigt is erger dan geen test — de volgende die er komt, leest hem als een besluit. Vraag voor de review: waar staat `.url()` nog meer, en welke andere zod-validators beloven meer dan ze doen? | ~~Middel~~ opgelost |
| 2026-08-23 | De testharnas liet elke run een groep achter | ✅ **Gedicht in `harness.ts`.** Opgelopen tot zeventig wezen-groepen op 22-08, plus 22 achtergebleven accounts op 23-08. Twee losse mechanismen: (1) `createTestProfile()` registreerde zijn gebruikers niet in de boekhouding, dus elf accounts per run lekten — dat was al gedicht in deze branch; (2) `besluiten.test.ts` verwijdert `dave` als onderdeel van A3, `groups.created_by` gaat via ON DELETE SET NULL op NULL, en daarna vindt `wipe('groups','created_by')` die groep niet meer terug. **Waarom dit een reviewpunt is en geen opruimklusje:** de suite draait tegen het echte project met een key die RLS omzeilt, en opruimen dat op een lijst in het geheugen leunt, faalt precies wanneer het misgaat — bij een crash. Zolang er geen tweede project of lokale stack is (zie `2026-08-22-rls-suite-tegen-productie.md`), blijft dat de structurele zwakte. | ~~Middel~~ opgelost |
| 2026-08-15 | `shared/time` | Twee klokken naast elkaar: persoonlijke cyclus én groepsperiode. Dit is de duurste keuze in het project. Als de scheiding niet scherp blijft, sijpelt de ene klok de andere in en wordt elke week-afhankelijke query verdacht.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** De duurste keuze van het project, en er is geen stand die "scherp genoeg gescheiden" bevestigt. Wat er wél is: `currentUserCycle()` en `currentGroupPeriod()` bestaan naast elkaar sinds dag één, een eslint-regel verbiedt tijdrekenen buiten `shared/time`, en elke week-afhankelijke feature wordt met twee verschillende week-starts getest. Of dat volstaat, is een gesprek. | Hoog |
| 2026-08-15 | `docs/decisions/001-datamodel.md` | `weekly_goals.status` is een cache van `completions` + `completion_approvals`. Bewuste denormalisatie voor lijstschermen. Loopt hij uit de pas, dan klopt de UI niet meer met de waarheid. Er moet een herstelscript zijn en een test die de cache tegen de gebeurtenissen aan houdt.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Een ontwerpvraag over denormalisatie, geen defect. Of de cache uit de pas loopt, blijkt pas uit gebruik — `herbereken_reeks()` is de herbouwweg en die werkt. | Middel |
| 2026-08-15 | `completion_approvals` | Zelfgoedkeuring wordt geblokkeerd via een `CHECK` op een **gedenormaliseerde** `subject_id`, omdat een `CHECK` geen subquery mag doen. Een trigger vult en bewaakt die kolom. Als de trigger fout gaat, verdwijnt de belangrijkste autorisatiegrens in de app stilletjes.  📏 **Nagemeten op 25-08-2026 en staat nog.** De gedenormaliseerde `subject_id` staat er nog, met `fill_approval_subject()` als BEFORE-trigger die hem vult. De constructie werkt en wordt getest; wat een reviewer moet wegen is of een CHECK op een gedenormaliseerde kolom hier de juiste vorm is, en dat is een oordeel en geen meting. | Hoog |
| 2026-08-15 | RLS-hulpfuncties | `is_group_member()` en `shares_group_with_goal()` zijn `SECURITY DEFINER` om RLS-recursie op `group_members` te vermijden. `SECURITY DEFINER` omzeilt RLS — deze twee functies moeten regel voor regel nagelopen worden.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** `is_group_member()` en `shares_group_with_goal()` zijn nog steeds `SECURITY DEFINER`, en dat moet ook — zonder dat krijg je RLS-recursie op `group_members`. De vraag is of die keuze de juiste is en waar hij nog meer doorwerkt; dat is precies het soort ding waarvoor de review bestaat. | Hoog |
| 2026-08-15 | Risico-radar (EPIC 12) | De haalbaarheidsberekening is een heuristiek die ik zelf bedacht heb. Er is geen validatie dat de vier standen iets zinnigs zeggen. Bij een verkeerde "deadline onhaalbaar" verliezen we vertrouwen op precies het verkeerde moment.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Een heuristiek valideer je met gebruikersdata, en die is er niet. Dit is een gespreksonderwerp voor november en geen bevinding die dichtgezet kan worden. | Middel |
| 2026-08-15 | Twee ritmes (Dagzet + weekcyclus) | Productbeslissing, geen technische. Als gebruikers de Dagzet gaan zien als "de check-in die telt", ondermijnt dat de weekcyclus. Meten hoe dit uitpakt vóór er meer op de Dagzet gebouwd wordt.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Productvraag. Of gebruikers de Dagzet als "de check-in die telt" gaan zien, blijkt uit gedrag en niet uit het schema. | Middel |
| 2026-08-15 | Groepsoverzicht (QS8-55) | Klassieke N+1. Eén query over `group_members → goal_group_links → goals → user_streaks` met een `LATERAL` op `weekly_goals`. Moet met een queryteller getest worden, niet op het oog.  📏 **Nagemeten op 25-08-2026 en staat nog.** De vorm staat er nog (één query met een `LATERAL`), en er is nog steeds geen queryteller in de tests. ⚠️ **Maar de kosten zijn nu gemeten:** warm 4,6–6,9 ms bij 50 leden, 16,9–18,7 ms bij 200. Zie de rij over `group_overview()` onderaan voor de meting en de valkuil erin (de eerste, koude meting gaf een factor acht te hoog). | Middel |
| 2026-08-15 | `supabase/migrations/0004` | **Bewust geaccepteerd risico.** Vier functies blijven aanroepbaar door ingelogde gebruikers en de advisor blijft daarover waarschuwen: `is_group_member`, `shares_group_with_goal`, `shares_group_with_user` en `join_group_with_code`. De eerste drie moeten dat zijn — RLS-policies roepen ze aan namens de gebruiker. Ze geven alleen een boolean over de aanroeper zelf terug. `join_group_with_code` is per definitie publiek voor ingelogde gebruikers. Laten nakijken of een aanvaller met deze drie booleans het bestaan van groepen of doelen kan aftasten (uuid-enumeratie).  📏 **Nagemeten op 25-08-2026 en staat nog.** Alle vier de functies zijn nog uitvoerbaar door `authenticated` — nagemeten met `has_function_privilege()`. Dat is nog steeds het bewust geaccepteerde risico dat deze rij beschrijft, niet iets nieuws. | Middel |
| 2026-08-15 | RLS-testdekking | De rooktest van 15-08 draaide als `service_role` en heeft dus alleen constraints en triggers geraakt, **niet de policies zelf**. QS8-98 heeft daar op 16-08 een suite voor gebouwd met echte JWT's. **Gedeeltelijk opgelost: de kern is gedekt, de rest niet.** Ongetest blijven `milestones`, `goal_events`, `goal_group_links`, `week_reviews`, `chain_links`, `breathers`, `commitment_events` en de schrijfkant van `profiles` en `groups`. Ook onbewezen: de clausule `c.user_id <> auth.uid()` in `completion_approvals_insert` — de trigger gooit er altijd eerder overheen, dus haal je die clausule weg dan blijft de suite groen. Deze regel gaat pas van de agenda af als die vier erbij staan.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Deels achterhaald — de suite is inmiddels ruim 350 tests en draait mee in CI — maar welke tabellen nog ongedekt zijn, is een oordeel over dekking en geen meetbare stand. Blijft agenda. | **Middel** |
| 2026-08-16 | `group_visible_streaks` | **Defect, gevonden door QS8-98.** De view staat op `security_invoker = true` en draait dus onder de RLS van de aanroeper. De policy op `user_streaks` is `user_id = auth.uid()`, waardoor de view iedereen uitsluitend zijn eigen reeks toont — het omgekeerde van de bedoeling. Het groepsoverzicht (QS8-55) en De Ketting (QS8-80) hangen hieraan. Gedicht in 0005: `security_invoker = false`, `security_barrier = true`, en `g.owner_id = s.user_id` erbij zodat de view op zichzelf correct leest. ⚠️ Ná 0005 is de kolomlijst van de view de énige bescherming van `total_points`. | ~~Hoog~~ opgelost |
| 2026-08-16 | Groep aanmaken | **Defect, gevonden door QS8-98.** Een groep aanmaken kán niet vanuit de client. `groups_select` eist `is_group_member(id)`, en `group_members_insert_founder` toetst het oprichterschap met een subquery op `groups` die zelf ook onder RLS draait. De oprichter moet dus lid zijn om zijn groep te zien, en zijn groep zien om lid te worden. Bovendien geldt de SELECT-policy óók voor de `RETURNING`-rij van een INSERT, wat de misleidende melding "new row violates row-level security policy" oplevert. Opgelost in 0009 met de RPC `create_group`, die groep én oprichterslidmaatschap in één transactie zet. De RLS-suite gebruikt hem nu via de gewone client, wat meteen het bewijs is dat het gat dicht is. | ~~Hoog~~ opgelost |
| 2026-08-16 | Account verwijderen | **Defect, gevonden door QS8-98.** `delete auth.users` faalt met "Database error deleting user" zodra de gebruiker iets heeft aangemaakt. `profiles` hangt met cascade aan `auth.users`, maar `groups.created_by`, `completions.user_id`, `completion_approvals.approver_id`/`subject_id`, `chat_messages.sender_id` en `goal_events.actor_id` verwijzen naar `profiles` **zonder** cascade. Dat raakt niet alleen de tests: een gebruiker die zijn account opzegt loopt tegen dezelfde muur, en dat is een AVG-verplichting. Vraagt een besluit per kolom — cascade of anonimiseren. Bij `completion_approvals` is anonimiseren waarschijnlijk juister: een goedkeuring is bewijs voor iemand ánders.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Aangetoond in een teruggedraaide transactie op het echte project: een account met een groep, een gekoppeld doel, een weekdoel, een chatbericht én een `goal_events`-rij wordt zonder fout verwijderd. De cascades en `on delete set null` doen wat ze beloven. ⚠️ De val die dit ooit brak is sinds 0086 een controle — `onveranderlijkheid_bewaking()`. | ~~Hoog~~ opgelost |
| 2026-08-16 | `group_members_update` | ✅ **Gedicht in migratie 0006, 16-08.** Was: De policy heeft geen `with check`; Postgres gebruikt dan de `using`-expressie ook als check, en die eist alleen `user_id = auth.uid()`. De kolom `role` staat nergens vast. Wie via een geldige uitnodigingscode binnenkomt, maakt zichzelf met één update admin — en kan daarna via `groups_delete` de hele groep met alle geschiedenis wissen of via `group_members_delete` iedereen eruit zetten. Reparatie: expliciete `with check` plus een `before update`-trigger die `role` en `group_id` terugzet voor niet-admins. Reparatie: expliciete `with check` plus een `before update`-trigger die `role` en `group_id` terugzet voor niet-admins. Bewezen door de test in `tests/rls/policies.test.ts`. | ~~Hoog~~ opgelost |
| 2026-08-16 | `chat_messages_update` | ✅ **Gedicht in 0006 en 0010, 16-08.** Was: De INSERT-policy blokkeert `type = 'system'`, de UPDATE-policy niet. Een lid plaatst dus een gewoon bericht en werkt het daarna bij naar een systeembericht met een verzonnen `system_event`. Systeemberichten zijn het kanaal dat de groep vertrouwt; dit is de directe route om domeinregel 7 van buitenaf te breken. De `with check` mist bovendien `is_group_member(group_id)`, dus hetzelfde bericht is naar een willekeurige andere groep te verplaatsen. Ook `created_at` was client-instelbaar bij INSERT, waardoor het bewerkvenster van 15 minuten geen venster was. 0010 maakt een bericht onveranderlijk op `body` en `attachment_url` na. | ~~Hoog~~ opgelost |
| 2026-08-16 | Client-gekozen waarden | ✅ **Alle vier gedicht in 0006 en 0007, 16-08.** Waren: (1) `commitments_insert` controleert `status` en `beneficiary_group_id` niet: een straf is direct als `due` aan te maken, waarna een willekeurige groep leesrecht krijgt — botst frontaal met domeinregel 11. (2) `daily_moves_write` controleert `weekly_goal_id` niet: tekst plaatsen in de doeldraad van een ander. (3) `points_ceiling`/`points_floor`/`points_miss` zijn onbegrensd, waardoor de score betekenisloos wordt (domeinregel 10). (4) `completions.cycle_start_date` wordt nergens tegen het weekdoel gehouden — week-backdating. Alle vier zijn `check`-constraints of triggers geworden; RLS kan dit niet. Wat 0006 leerde: `chat_messages` bleef falen ná de reparatie, omdat `is_group_member` slaagt voor een groep waar je zélf ook in zit — 0010 pint de kolommen daarom vast in een trigger. | ~~Hoog~~ opgelost |
| 2026-08-16 | `weekly_goals_select` | ✅ **Gedicht in 0019 en 0020, gevonden door de security-review van EPIC 5.** Was: de policy gaf elke groepsgenoot de héle rij van een gekoppeld doel, inclusief de kolom `status`, die letterlijk `'missed'` kan zijn. Eén `GET /rest/v1/weekly_goals?select=status` leverde de volledige lijst gemiste weken van een ander op, met datum. `001-datamodel.md` §4.3 belooft "ook niet door slim te bevragen"; er was geen slimheid voor nodig. Dezelfde rode draad als 0006/0010/0016 (RLS beslist over rijen, niet over kolommen), maar dan aan de leeskant — en die kant had niemand nagelopen. 0019 was te grof (alleen `pending`/`approved`) en brak goedkeuring én de gedeelde Dagzet; 0020 sluit `missed` en `carried` uit en laat de rest staan. ⚠️ Wat blijft: uit de afwezigheid van een weekdoel in een cyclus is nog iets af te leiden. Dat is dubbelzinnig en veel zwakker, maar het is niet nul. | ~~Kritiek~~ opgelost |
| 2026-08-16 | `goals.risk_status` en `risk_reason` | ✅ **Gedicht in migratie 0050, 20-08, vóór EPIC 12 de kolom ging vullen.** De drie risicokolommen zijn verhuisd naar een eigen tabel `goal_risk` met eigenaar-only RLS. Niet via een kolomgrant (die geldt per rol, dus de eigenaar raakt zijn eigen stand kwijt) en niet via `goals_select` terug naar eigenaar-only (dat breekt `group_overview()`, die `STABLE` is en geen definer, en dus `goals` onder de RLS van de aanroeper leest). Een eigen tabel maakt de bescherming **structureel**: er is geen rij om te lekken en dus geen kolomlijst die iemand later per ongeluk uitbreidt. Beslisdocument 002 §4a vroeg hier zelf om — "herbevestigen vóór EPIC 12" — en Quinten heeft herbevestigd met de uitkomst "dicht". Was: **Zelfde soort gat, bewust niet in EPIC 5 gerepareerd.** `goals_select` geeft groepsgenoten de hele rij van een gekoppeld doel, dus ook `risk_status` (`'behind'`, `'unreachable'`) en de jsonb-onderbouwing in `risk_reason`. Dat zijn per definitie tegenslagsignalen over iemand anders. Repareren vraagt `goals_select` terug naar eigenaar-only plus een definer-view met kolomlijst voor de groep, en dat raakt EPIC 6, 7 en 12 én de policy-matrix in het beslisdocument — een architectuurwijziging, geen reparatie. **Vandaag niet misbruikbaar**: de Risico-radar bestaat nog niet en `risk_status` staat op elke rij op `on_track`. Dat verandert op de dag dat EPIC 12 begint. | ~~Hoog~~ opgelost |
| 2026-08-16 | `group_visible_streaks.best_streak` | ✅ **Gedicht in 0019.** De view gaf groepsgenoten `best_streak` naast `current_streak`, en `best_streak > current_streak` is sluitend bewijs dat iemand een reeks verbroken heeft. §4.3 zegt met zoveel woorden dat de groep **uitsluitend** `current_streak` mag zien; 0005 licht de projectie regel voor regel toe en liet hem er toch in staan. De test op 0016 controleerde `total_points` en `last_cycle_start` en liet hem er precies langs. | ~~Kritiek~~ opgelost |
| 2026-08-16 | `invite_preview` en toestemming | ✅ **Ingeperkt in 0019, maar het blijft jouw keuze (Q-TODO A16).** De functie gaf aan `anon` de volledige weergavenaam, avatar-URL en gekoppelde doeltitel van maximaal acht leden. De redenering in 0016 ("koppelen is de toestemming; deze functie rekt die niet op") klopte binnen zijn eigen kader en was toch verkeerd: koppelen is toestemming voor de gróép, en een uitnodigingslink is nu juist bedoeld om doorgestuurd te worden. Codes verlopen bovendien nooit. Zonder account nu: groepsnaam, aantal, huddledag en voornamen. | ~~Kritiek~~ ingeperkt |
| 2026-08-16 | ✅ `is_group_member` en `is_group_admin` negeren `status` | 0008 en 0017 beschrijven `status = 'inactive'` allebei als moderatiemiddel, maar de hulpfuncties kijken alleen of de rij bestaat. Een uitgezet lid leest dus nog steeds de groep, de chat, de ledenlijst en — via `groups_select` — de geldende `invite_code`; was hij admin, dan kan hij de code nog roteren en de groep verwijderen. Reparatie is `and status <> 'inactive'` in beide functies, maar dan moet apart bekeken worden wat er met historische rijen (`chain_links`, `week_reviews`) gebeurt: toegang intrekken mag, geschiedenis herschrijven niet.  ✅ **Al gerepareerd; deze rij was alleen nooit bijgewerkt.** Nagemeten op 25-08-2026 met `pg_get_functiondef()`: beide functies dragen `and status <> 'inactive'`, en `groups_select` is `is_group_member(id)` — dus een uitgezet lid leest de groep en de uitnodigingscode niet meer. ⚠️ **Dat de rij bleef staan is zelf de les.** De risicokolom van dit document volgt de reparatie niet automatisch, dus een gesloten bevinding blijft er als open uitzien — en dan lees je hem drie keer opnieuw. Bij twijfel: meet de gedeployde stand, niet het document. | ~~Hoog~~ opgelost |
| 2026-08-16 | `groups_delete` en de dagteller | Twee dingen die aan elkaar hangen. (1) Elke beheerder kan de groep verwijderen, en dat cascadeert naar `group_members`, `goal_group_links`, `chain_links`, `week_reviews` en `chat_messages` — onomkeerbaar, zonder audit, zonder backups op de gratis tier. Botst met §2.5 ("het vertrek van een lid mag de historische ketting niet wijzigen") en met domeinregel 6. (2) Daardoor is de dagelijkse limiet op `create_group()` te omzeilen: hij telt rijen die er nóg zijn, dus aanmaken-verwijderen-opnieuw is ongelimiteerd. Beide opgelost door `groups_delete` te vervangen door een `archived`-status, of door de teller in een append-only tabel te zetten.  ✅ **Gedicht in 0092 op 25-08-2026.** `groups_delete` staat op `using (false)` en er is een `archived`-status met `archiveer_groep()`: actieve beheerder, expliciete bevestiging, een rij in `group_events`, en niets wordt gewist. Deel (2) lost zichzelf daarmee op — de dagteller in `create_group()` telt rijen die nu niet meer kunnen verdwijnen. ⚠️ **Eén slot en niet tien.** Er lopen tien schrijfpolicies langs `is_group_member()` of `is_group_admin()` (geteld in `pg_policies`, niet geschat). De archieftoets zit daarom in die twee functies zelf: alle tien sluiten in één keer, en de elfde die er volgend jaar bijkomt doet vanzelf mee. ⚠️ **En er waren vier routes terug uit het archief**, gevonden door in `pg_proc` te zoeken naar elke functie die `update groups` doet — niet door te bedenken welke het zouden kunnen zijn. De gevaarlijke was `join_group_with_code()`, die eindigt met `set status = 'active'`: iedereen met de uitnodigingscode kon een gearchiveerde groep zo terugzetten. Eén trigger (`archief_blijft_archief`) dekt alle vier, en geldt bewust óók voor `service_role` en definer-functies — drie van de vier zíjn definer-functies, dus de rolfilter van `guard_group_update()` zou hier juist het gat zijn geweest. ⚠️ **De kost:** een gearchiveerde groep verdwijnt ook uit bééld voor zijn leden, want `groups_select` loopt langs dezelfde functie. Beter dan vandaag (waar de groep echt wég is), maar geen leesbaar archief. Zie de rij hieronder. ⚠️ **Twee tellingen van hetzelfde plafond liepen uiteen**, gevonden doordat een test het ene pad toetste en het andere niet: `create_group()` sloot gearchiveerde groepen uit van de tien-groepen-grens en `join_group_with_code()` niet. Een limiet die van je route afhangt is geen limiet; nu rekenen ze hetzelfde. Bewezen in `tests/rls/archief.test.ts` (negen gevallen) en met de hand rood gemaakt: de archieftoets uit de twee hulpfuncties halen en de trigger droppen maakt vier van de negen rood — precies de vier die die sloten bewaken. | ~~Middel~~ opgelost |
| 2026-08-16 | `p_period_start` is vrij te kiezen | `group_overview()` neemt de periode als parameter, en de app vraagt uitsluitend de lopende op — maar dat is een UI-afspraak, geen beperking. Elk lid kan hem in een lus over vijftig weken zetten en zo de gaten in andermans Ketting uitlezen. `chain_links` is bovendien sowieso direct leesbaar voor leden. Dát De Ketting groepszichtbaar is, is een bewuste keuze (§2.5); dat de gáten erin dat ook zijn, is nooit expliciet besloten. Kiezen: server-side beperken tot de lopende en vorige periode, of de keuze vastleggen en de misleidende comments weghalen.  ✅ **Nagemeten op 25-08-2026.** `group_overview()` grenst de periode inmiddels af — er staat een venster op `p_period_start`, dus het is geen vrije parameter meer. | ~~Middel~~ opgelost |
| 2026-08-17 | `dien_opnieuw_in` en twee foute aannames over Postgres | Beide gevonden door een test, geen van beide door de code te lezen. (1) Een `BEGIN ... EXCEPTION`-blok in PL/pgSQL draait alleen zíjn eigen werk terug, niet wat ervóór in de functie gebeurde — de eerste versie liet daardoor een weekdoel achter met nul actieve voltooiingen. (2) De truc om de oude rij tijdelijk naar zichzelf te laten wijzen is sinds 0001 verboden door `completions_no_self_supersede`, en terecht: dat is geen "vervangen door" maar een lus. 0022 draait de volgorde om zodat alle tussenstanden geldig zijn. **De les die blijft: elke aanname over wat Postgres doet, hoort een test te hebben** — dit is na de `raise exception`-vondst van 0017 de tweede keer dat een redenering over transacties niet klopte. | ~~Hoog~~ opgelost |
| 2026-08-17 | `weekly_goals.status` was client-schrijfbaar | ✅ **Gedicht in 0023, gevonden door de security-review op EPIC 6, en het was de kern van domeinregel 3.** Goedkeuren zelf was drievoudig op slot, maar het *effect* ervan — `status = 'approved'` — was met één PATCH-verzoek door de eigenaar te zetten. Geen buddy nodig; de reeks die de groep van je ziet was daarmee zelf te verzinnen. Dezelfde route gaf `excused` (een gratis adempauze zonder weekpas) en herstelde een week die de rollover al op `missed` had gezet, mét minpunt. **De les: een autorisatiegrens is pas dicht als ook het gevólg ervan op slot zit.** `completion_approvals` was perfect beveiligd en volstrekt omzeilbaar. | ~~Kritiek~~ opgelost |
| 2026-08-17 | Goedkeuring op een ingetrokken voltooiing | ✅ **Gedicht in 0023.** `completion_approvals_insert` toetste niet op `superseded_by is null`, dus een tweede buddy kon punten toekennen op een bewering die de eigenaar zelf al had ingetrokken en gecorrigeerd. `openstaande_beoordelingen` filterde hem wél — het scherm deed het goed, de policy niet. Vierde keer dat dit patroon terugkomt, na 0006, 0007 en 0019. | ~~Kritiek~~ opgelost |
| 2026-08-17 | Reviewpunten waren in een lus te verdienen | ✅ **Gedicht in 0023.** Twee accounts in een eigen groep met `evidence_policy = 'optional'`: opnieuw indienen geeft een nieuwe `completion_id`, de ander boekt `more_info` (+1), herhaal. Twee verzoeken per punt, en onbegrensde rijgroei op een gratis tier van 500 MB. Nu valt een reviewpunt alleen op een actieve voltooiing van een week die nog op een oordeel wacht. | ~~Middel~~ opgelost |
| 2026-08-17 | Bewijseis te omzeilen met ontkoppelen | `enforce_evidence_policy` kijkt naar de groepskoppelingen op het moment van invoegen, en de eigenaar mag `goal_group_links` zelf verwijderen en aanmaken. Drie verzoeken: koppeling weg, voltooiing zonder notitie invoegen, koppeling terug. "De strengste eis wint" is de juiste regel op het verkeerde moment — hij hoort (ook) getoetst te worden bij het invoegen van de goedkeuring. **Bewust niet gerepareerd:** dit is zelfbedrog en geen autorisatiegrens; wie dit doet, ontneemt zijn eigen buddy de kans om te reageren. Repareren kost een tweede afdwingpunt in het goedkeuringspad en dat verdient een bewuste keuze. **Wordt zwaarder als:** een beslissing op de koppelstand gaat leunen. ⚠️ **Dat is op 22-08 gebeurd** — 0064 liet het minpunt afhangen van de koppeling op het moment van boeken, en daarmee werd dezelfde handeling een scoregat. Gedicht in 0066. Dit is de rij die QS8-123 heeft opgeleverd. | Laag |
| 2026-08-17 | Realtime publiceert ook DELETE | `alter publication ... add table` publiceert alle bewerkingen, en Supabase past RLS **niet** toe op DELETE-events. Elke ingelogde gebruiker kan zich abonneren op verwijderingen in `completions` en `weekly_goals` van het hele project. Vandaag lekt dat alleen een uuid en een tijdstip, want `replica identity` staat op DEFAULT. **Het wordt een lek van de eerste orde zodra iemand `replica identity full` zet** — de standaardtruc om `old_record` te krijgen — want dan gaat de volledige oude rij mee, inclusief `status = 'missed'` en de notitie. Leg dat vast als verbod op deze twee tabellen. `publish` is een publicatie-optie en niet per tabel in te stellen, dus beperken kan alleen door de publicatie te splitsen. ✅ **Sinds EPIC 7 is het verbod vastgelegd én getest.** Het staat in `CLAUDE.md` bij domeinregel 7 (dat was Q-TODO A20), `chat_messages` is de derde tabel die eronder valt, en `realtime_bewaking()` (migratie 0027) maakt er een test van in `tests/rls/epic7.test.ts`: geen enkele uitgezonden tabel mag op `full` staan. Wat blijft: het is nog steeds geen slot maar een test, en de app abonneert zich nergens op DELETE. | ~~Middel~~ vastgelegd en getest |
| 2026-08-17 | `completion_approvals_select` is te ruim | Élk groepslid leest élke `more_info`-opmerking over élk ander lid. "Anna is doorgevraagd" is een licht negatief signaal over iemand anders dat niet via die persoon zelf loopt — de rand van domeinregel 7. `fetchVragen` heeft niet meer nodig dan `approver_id = auth.uid() or subject_id = auth.uid()`. **Wordt zwaarder als:** een `more_info`-opmerking ergens getoond wordt buiten de vrager en de gevraagde om, of zodra groepen groter zijn dan een handvol mensen. | Laag |
| 2026-08-17 | Goedkeuren is onomkeerbaar en vraagt niets | Twee kaarten onder elkaar op een telefoon, één dikke duim, en je hebt de verkeerde buddy goedgekeurd. Punten geboekt, reeks herberekend, weekdoel op `approved` — en `completion_approvals` heeft geen UPDATE- en geen DELETE-policy, dus terugdraaien kan alleen met de hand in de database. Append-only is hier de juiste keuze (domeinregel 6), maar dan hoort er een venster van een paar seconden vóór het boeken te zitten, of een bevestiging. Nu is er geen van beide.  📏 **Nagemeten op 25-08-2026 en staat nog.** De helft "onomkeerbaar" klopt niet meer: `trek_goedkeuring_in()` bestaat (A19, binnen een kwartier). Wat wél staat is "vraagt niets" — er zit geen bevestiging tussen de duim en de boeking, en dát was het scenario. | Middel |
| 2026-08-17 | Beoordelen is alleen zichtbaar als je ernaar zoekt | De enige ingang naar het beoordeelscherm is een kaart op het groepstabblad. Er is geen badge op de tabbalk en geen notificatie — dat laatste is EPIC 11 en staat dus ingepland, maar tot die er is hangt de succesmetriek uit de PRD (≥80% goedgekeurd binnen 48 uur) volledig aan de vraag of iemand toevallig op Groep tikt. Bij het bouwen van EPIC 11: dit is de eerste melding die er moet zijn, vóór alle herinneringen aan jezelf.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** EPIC 11 heeft de notificatiekant gebouwd, maar of de ingang goed genoeg vindbaar is, weet je pas als er gebruikers zijn. | Middel |
| 2026-08-17 | Reviewpunten en misbruik | `award_points_on_approval` boekt sinds 0021 een punt voor de beoordelaar, ook bij "vertel me meer" (6.6). De dedupe-index zorgt dat het per voltooiing één keer telt en de CHECK blokkeert zelfgoedkeuring, maar twee accounts die elkaars weken blijven indienen en beoordelen, kunnen elkaars buddy-bijdrage wél opblazen. Vandaag onschadelijk (het getal staat alleen op je eigen profiel en telt niet mee in reeks of doelvoortgang), maar zodra er een ranglijst of beloning aan hangt, is dit de eerste route.  📏 **Nagemeten op 25-08-2026 en staat nog.** `award_points_on_approval()` boekt `review_given` zodra het weekdoel `pending` is, ongeacht `new.status` — dus ook bij "vertel me meer". Precies zoals deze rij beschrijft; de dedupe-index (`on conflict do nothing`) is nog steeds de enige rem. | Middel |
| 2026-08-21 | De Doelcoach rekende zelf met datums, en deed dat fout | Bij de eerste proef met de tegenspraak (QS8-38) gaf het model een juiste conclusie met een verkeerde onderbouwing: "ongeveer 14 maanden" voor een streefdatum die twee weken weg lag. Gerepareerd door het aantal weken en de totale beschikbare uren server-side uit te rekenen (`daysBetween` uit `shared/time`) en mee te geven in de prompt; opnieuw geverifieerd en toen exact. **De les is breder dan deze ene prompt: vraag een taalmodel nooit om rekenwerk dat je zelf kunt doen.** Bij elke volgende AI-functie (QS8-41, weekdoelen per mijlpaal) hoort dezelfde vraag gesteld te worden — welke getallen in dit antwoord kan ik zelf uitrekenen? Bijkomend voordeel dat de moeite waard is om te weten: het antwoord werd goedkoper en sneller (0,73 cent in 11s tegen 1,76 in 27s), omdat er minder te slagen viel om de arm.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Modelgedrag over veel calls; met drie proeven is er niets te meten. | Middel |
| 2026-08-21 | De Doelcoach is drie keer gedraaid, niet drieduizend | De keten werkt end-to-end en dat is nu aangetoond, maar met precies drie echte calls. Wat we níét weten: hoe vaak het model buiten het schema valt, hoe vaak `stop_reason` op `max_tokens` komt bij een doel met veel context, en of de kostenschatting (`PRIJS_PER_MTOK_CENT`) nog klopt met de actuele prijslijst. Die prijzen staan hardgecodeerd in de Edge Function; ze zijn niet verifieerbaar vanuit de code en verouderen stil. Laten nakijken: een tegen de prijslijst gecontroleerde constante, en of er iets van een alarm moet komen als `cost_cents` per week boven een grens komt. Nu is er wel een query (`ai_kosten_per_week`) maar niemand die hem draait.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Zegt het zelf: dit wordt pas een meting zodra er volume is. | Middel |
| 2026-08-21 | Meldingen leunen op een uurjob en niet op events | Een goedkeuringsverzoek kan tot een uur oud zijn voordat de push komt: de job draait elk uur op het halve uur en scant de stand. Het alternatief is een trigger die `pg_net` gebruikt, en dan staat de service-role-key in de database — precies wat op 19-08 is afgewezen bij de rolloverplanning. Voor een app met een weekritme is een uur de goedkopere kant van die ruil, maar het is wél de reden dat de succesmetriek uit de PRD (≥80% goedgekeurd binnen 48 uur) niet op snelle meldingen kan leunen. Laten heroverwegen zodra er een tweede kanaal is (bijvoorbeeld realtime in de app zelf, die wél een sessie heeft). **Wordt zwaarder als:** er een melding bij komt die binnen een uur moet aankomen. Een weekritme verdraagt een uur; een straf die verschuldigd wordt of een chatbericht niet. | Laag |
| 2026-08-21 | Een pushtoken is over te nemen door wie hem kent | `registreer_push_token()` (0055) haalt een token weg bij de vorige eigenaar en zet hem op de aanroeper. Dat moet, anders is een gedeeld apparaat kapot — de vorige gebruiker blijft dan meldingen krijgen op een telefoon die niet meer van hem is. **Wat je ermee accepteert:** wie een token van een ander kent, kan het naar zich toe trekken; die persoon krijgt daarna zijn eigen meldingen niet meer. Een stille denial-of-service dus, geen datalek — de kaper krijgt zíjn meldingen op andermans toestel, niet andersom. De bescherming is de entropie van een Expo-token, precies zoals bij de uitnodigingscode (A16): entropie en geen slot. Een echte oplossing vraagt dat het token bewijsbaar van dat apparaat komt, en die mogelijkheid biedt Expo niet. Laten nakijken of dat nog steeds zo is. **Wordt zwaarder als:** er echte gebruikers zijn — dan bestaan er pas tokens om over te nemen — of zodra een melding meer draagt dan een naam en een gebeurtenis. | Laag |
| 2026-08-21 | ✅ De RLS-suite zat over de aanmeldlimiet — deels verholpen | Nagemeten in plaats van geschat: de volledige suite maakte **43** aanmeldingen tegen een limiet van ongeveer dertig per uur. Een volledige run zat dus niet marginaal maar structureel over de grens — hij slaagde alleen als het uur ervóór stil was geweest. Vier keer tegenaan gelopen op 20 en 21 augustus, en elke keer ziet het eruit als een kapotte policy terwijl het een limiet is. **Deels verholpen:** elf van die 43 zaten in één test (twaalf groepsleden voor een N+1-controle) en hoefden geen JWT. `createTestProfile()` maakt nu een gebruiker zonder aan te melden; totaal 43 → 31, alle tests blijven groen. ⚠️ **31 is nog steeds dicht tegen de grens.** De structurele keuze staat open en is bewust niet door mij gemaakt: accounts delen over suites heen (één gedeelde fixture), of de suite splitsen in een deel per wijziging en een deel per nacht. Beide raken de manier waarop je met de suite werkt. Let bij die keuze op de valkuil in `createTestProfile`: een fixture die RLS omzeilt om RLS te testen bewijst niets. Aanmeldingen sparen mag alleen bij opvulling.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Volledig verholpen door de lokale opstelling (QS8-119): `scripts/lokale-stack.sh` plus `schema-opbouwen.sh` bouwen het schema op een eigen database, en `shim_maak_gebruiker()` vervangt GoTrue. `RLS_DOEL=lokaal npm test` doet **nul** aanmeldingen bij Supabase. De echte sessie wordt nog getoetst in `token.test.ts`, en dat zijn er een paar. | ~~Middel~~ opgelost |
| 2026-08-20 | De haalbaarheidsberekening (`herbereken_risico`, 0051) | **Dit blijft een heuristiek die wij zelf bedacht hebben**, zonder onderzoek en zonder gebruikersdata — precies wat de rij hierboven over de Risico-radar al zei, nu met code eronder. De drempels (`achterstand_factor` 1,5 · venster 4 cycli · vloeraandeel 0,75 · minimaal 3 cycli geschiedenis) zijn beredeneerd en niet gemeten. Ze staan met naam bovenaan de functie, zodat verstellen geen herlezen vraagt. Er zijn zeven scenariotests plus een determinismetest, dus het gedrag ligt vast — maar dat de uitkomst *klopt* is niet getest en kan ook niet, zolang er geen gebruikers zijn. ⚠️ Het gevaarlijkste geval is een onterechte "deadline onhaalbaar": dat is de zwaarste stand, hij kleurt rood, en hij komt op het moment dat iemand toch al twijfelt. Daarom heeft die stand de strengste eis — hij treedt alleen op bij een feit (de datum is er, of er zijn meer mijlpalen dan weken) en nooit op grond van tempo. Laten nakijken zodra er echte cycli in zitten.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Zelfde categorie als de Risico-radar hierboven: een zelfbedachte heuristiek zonder ijkpunt. | Middel |
| 2026-08-20 | ✅ `authenticated` heeft TRUNCATE en TRIGGER op alle 28 tabellen | Gevonden bij het intrekken van de schrijfrechten op `breathers` (0048). **TRUNCATE is niet onderworpen aan RLS**: een rol die het heeft, leegt de tabel ongeacht welke policy erop staat. Dat geldt hier voor `points_ledger`, `completions`, `chat_messages`, `chain_links` — alles. TRIGGER laat in principe een trigger op andermans tabel zetten. **Vandaag niet bereikbaar via de API**: PostgREST doet alleen SELECT/INSERT/UPDATE/DELETE en RPC, en geen DDL of TRUNCATE. Het is dus geen open deur maar een deur zonder slot in een muur waar (nu) geen gang achter zit. Het wordt wél bereikbaar zodra iemand met de rol `authenticated` een directe verbinding krijgt, of zodra er een SECURITY INVOKER-functie komt die dynamische SQL uitvoert. Reparatie is `revoke truncate, trigger on all tables in schema public from authenticated`, plus hetzelfde in de default privileges zodat nieuwe tabellen het niet opnieuw krijgen. **Niet gedaan**: dit raakt de rechten van elke tabel tegelijk en dat is een auth-wijziging, dus Quintens besluit (Q-TODO A46). Vermoedelijk een Supabase-standaard (`grant all on all tables`) en geen bewuste keuze — vergelijk de rij over migratie 0004.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Gedicht in 0073: `information_schema.table_privileges` geeft voor `authenticated` **nul** TRUNCATE- en TRIGGER-rechten. `npm run verbindingen:controle` en `ddl_rechten_in_de_api()` bewaken het sindsdien. | ~~Middel~~ opgelost |
| 2026-08-20 | `schuifDoor()` is niet atomair | Twee aanroepen zonder transactie eromheen: `markeer_doorgeschoven()` zet de oude rij op `carried`, daarna maakt `maakWeekdoel()` de nieuwe week. Valt de verbinding daartussen weg, dan staat de oude week op `carried` zonder opvolger, en hij verdwijnt uit het blok "Nog open van eerdere weken" omdat dat alleen `missed` ophaalt. Geen puntenverlies — de week telde al als gemist en het minpunt was geboekt — maar het weekdoel is weg zonder dat iemand het aanmaakte, en dat is niet terug te draaien vanuit de app. **Sinds QS8-106 voor het eerst bereikbaar**; daarvóór riep geen scherm deze functie aan. Samenvoegen tot één RPC die beide doet is de reparatie. Let daarbij op de les van 0017: in een SECURITY DEFINER-RPC overleeft niets een `raise exception`.  ✅ **Gedicht in 0091 op 25-08-2026.** `schuif_weekdoel_door()` doet het zetten op `carried` én de opvolger in één RPC, en PostgREST draait elke RPC in zijn eigen transactie. `markeer_doorgeschoven()` is gedropt in plaats van blijven staan — een dode achterdeur die een rij op `carried` kan zetten en voor `authenticated` aanroepbaar is, is hier duur. ⚠️ **Deze samenvoeging introduceerde zélf een lek, en dat is het leerzame deel.** Een SECURITY DEFINER-functie loopt om `weekly_goals_insert` heen en dus om de dagelijkse bovengrens van 0083 — twee correcte onderdelen, en de naad ertussen open. De toets staat nu ook in de RPC, en om het getal 200 niet nóg een keer op te schrijven vervangt `weekdoelen_over()` (resterend budget, vorm van 0090) de `weekdoelen_vandaag()` uit 0083; de policy en de RPC lezen allebei diezelfde functie. ⚠️ **De cyclus blijft van de client komen** — correctheidsregel 7: de database kent de week-startdag van deze gebruiker niet en hoort die niet uit te rekenen. Dezelfde verdeling als in `maakWeekdoel()`, dus geen nieuw vertrouwen. En passant komt de inhoud van de nieuwe week nu uit de oude rij in plaats van uit wat het scherm toevallig meestuurde. Bewezen in `tests/rls/doorschuiven.test.ts` (zes gevallen) en met de hand rood gemaakt: een lokale versie die de insertfout opvangt in plaats van hem te laten rollen — precies de uitkomst van vóór 0091 — laat de atomiciteitstest omvallen, en een versie zonder de bovengrens laat de tweede naadtest omvallen. | ~~Middel~~ opgelost |
| 2026-08-20 | ✅ `weekly_goals_select` en `excused` | **Derde exemplaar van hetzelfde patroon, gevonden tijdens QS8-106 en geverifieerd tegen de gedeployde database.** De policy schermt `missed`, `carried` en `cancelled` af voor groepsgenoten; `excused` niet, en er is geen kolomgrant of view die `status` inperkt. ⚠️ **Correctie op mijn eerste versie hiervan, en hij maakt het ernstiger.** Ik schreef "geen enkele functie schrijft `excused`, QS8-82 wordt de eerste", op grond van een zoektocht door `pg_proc`. Dat is de verkeerde plek: **de rollover is een Edge Function**, en `supabase/functions/rollover/index.ts` zet een weekdoel al op `excused` zodra er een adempauze over de cyclus loopt. Die job draait elk uur. De schrijver bestond dus al; wat ontbrak was invoer, want er is geen enkele manier om een rij in `breathers` te krijgen. Nul `excused`-rijen betekende "geen munitie", niet "geen code". **Dit is de valkuil dat `supabase/functions/` buiten typecheck, lint én CI valt, nu in reviewvorm: wie in de database zoekt, ziet die schrijver niet.** Voor de volgende keer: zoek een schrijver in `pg_proc` én in `supabase/functions/`. Dit is dezelfde vorm als de rij over `goals.risk_status` hierboven: een latent lek dat scherpstaat zodra de bijbehorende epic begint. En het is opnieuw "de schermen doen het goed, de database niet" — `rangeState()` verbergt `excused` netjes voor de groep en `metrics.test.ts` rekent hem tot de tegenslag. Reparatie is één woord in de array; **niet gedaan omdat RLS aanpassen Quintens besluit is** (Q-TODO A45). ⚠️ Er is geen test die dit dekt, en de bestaande test op `excused` (`policies.test.ts:2200`) gaat over de schrijfkant. Nieuwe test hoort een positieve controle te bevatten.  ✅ **Al gerepareerd door migratie 0047** (`0047_excused_hoort_bij_de_afscherming.sql` — de bestandsnaam zegt het), en ook deze rij was alleen nooit bijgewerkt. Nagemeten op 25-08-2026: de policy schermt `missed`, `carried`, `cancelled` én `excused` af, met sinds 0077 een derde tak voor open groepen. ⚠️ De aantekening in deze rij dat het "niet gedaan is omdat RLS aanpassen Quintens besluit is" is bovendien achterhaald: `CLAUDE.md` ruilde die lijst op 22-08 in voor twee grenzen, en RLS aanpassen is sindsdien een afweging die je zelf maakt en in het beslisdocument verantwoordt. | ~~Hoog~~ opgelost |
| 2026-08-16 | Rate limiting en `raise exception` | ⚠️ **De belangrijkste vondst van EPIC 5, en hij is breder dan één functie.** `join_group_with_code` schreef eerst een rij in `invite_events` en zocht daarna pas de code op — juist om mislukte pogingen te tellen. Die redenering klopt, de uitvoering niet: PostgREST draait elke RPC in zijn eigen transactie, en een `raise exception` rolt die terug inclusief de zojuist geschreven poging. De teller bleef dus op nul en de limiet van twintig gold in de praktijk alleen voor gelúkte toetredingen — precies het geval dat geen bescherming nodig heeft. Gedicht in 0017: de functie geeft nu een resultaat terug in plaats van te gooien. **De regel die hieruit volgt: in een SECURITY DEFINER-RPC overleeft niets een `raise exception`.** Elke rate limiter, auditregel of blokkade die hierna gebouwd wordt, moet in de happy path staan. Laten nakijken of dit patroon elders terugkomt. | ~~Hoog~~ opgelost, maar het patroon blijft |
| 2026-08-16 | ✅ `invite_preview` en anonieme rate limiting | `invite_preview()` is `SECURITY DEFINER` en aanroepbaar door de rol `anon` — het enige eindpunt van de app dat zonder account bereikbaar is, en dat is de hele bedoeling van QS8-59. Rate limiting per gebruiker kán daar niet: er ís geen gebruiker. De bescherming is de entropie van de code (12 tekens uit een alfabet van 30, ≈ 59 bits) plus de platformlimieten van Supabase. Laten nakijken of dat genoeg is, en of een limiet per IP op het edge-niveau haalbaar is. Bijkomend: wie de link heeft ziet de namen van de leden en de titels van hun gekoppelde doelen. Dat volgt uit 5.3 (koppelen ís de toestemming), maar het is de moeite waard om te bevestigen dat een lid dat begrijpt op het moment dat hij koppelt.  📏 **Nagemeten op 25-08-2026 en staat nog.** `pg_get_functiondef()` toont geen enkele limiet: de functie zoekt de groep, telt de leden en geeft de lijst terug — `invite_events` wordt niet geraakt. ⚠️ **En dit is de rij die mijn eigen eerste meting fout kreeg:** een regex op `invite_events|count(*)` meldde "limiet aanwezig", maar die `count(*)` telt de léden. Een grep is geen meting; lees de functie.  ✅ **Beantwoord op 25-08-2026, migratie 0088 — de rij vroeg om een controle en niet om een reparatie.** De entropie ís genoeg: alfabet 30, lengte 12, dus 30^12 = 531.441.000.000.000.000 ≈ **58,9 bits**. Bij duizend verzoeken per seconde kost de hele ruimte 16,8 miljoen jaar, en de vraag die er werkelijk toe doet — hoe lang tot je één bestáánde groep raadt — is bij een miljoen groepen nog altijd zeventien jaar onafgebroken hameren. ⚠️ **Een rate limit per IP zou daar niets aan toevoegen**, en dat is de uitkomst: er is geen gebruiker om per gebruiker te tellen, en tegen raden beschermt de entropie al met vele ordes marge. Wat een limiet wél doet is bronnen sparen, en dat is een platformtaak. ⚠️ **En de bijkomende zorg was al dicht zonder dat het hier stond:** `invite_preview()` heeft een `ingelogd`-tak — zonder sessie krijg je alleen de vóórnaam, geen avatar en geen doeltitel. Namen en doeltitels ziet alleen wie al een account heeft, en dát is 5.3. ⚠️ **Wat er wél gebouwd is: een slot op de sterkte.** `generate_invite_code()` gebruikt `gen_random_bytes` met verwerpingsbemonstering (`drempel = 240 = 8 × 30`, zodat de modulo-bias verdwijnt) — zorgvuldig werk dat nergens onder test stond. De bestaande test toetst de vórm, en twee wijzigingen halen die moeiteloos terwijl ze de sterkte slopen: `random()` in plaats van `gen_random_bytes`, of een ander alfabet zonder de drempel mee te nemen. `uitnodigingscode_bewaking()` maakt de drie getallen uitleesbaar en `policies.test.ts` bewaakt ze; beide breuken met de hand nagedaan en allebei rood. | ~~Middel~~ opgelost |
| 2026-08-16 | `guard_group_update` en `current_user` | De trigger die de uitnodigingscode, de oprichter en de slaapstand vastpint, onderscheidt client van systeem met `current_user not in ('authenticated','anon')`. Dat werkt omdat PostgREST van rol wisselt en een `SECURITY DEFINER`-functie als zijn eigenaar draait. Het is wél de eerste plek in dit project waar een beveiligingsbeslissing op een rolnaam leunt in plaats van op `auth.uid()`. Laten nakijken of Supabase die rolnamen garandeert, en of er een route is waarlangs een client onder een andere rol binnenkomt.  📏 **Nagemeten op 25-08-2026 en staat nog.** De vroege uitstap op de rolnaam staat er nog. ⚠️ **En op 25-08 bleek hij ergens de veiligheid te dragen:** `groups.created_by` heeft `on delete set null`, en dat de cascade daar langs kwam kwam juist dóór deze uitstap. Migratie 0086 heeft die afhankelijkheid weggehaald met de 0060-grendel, zodat het weghalen van deze regel geen stille vierde bug meer oplevert. | Middel |
| 2026-08-16 | ✅ `join_group_with_code` | Geen enkele limiet. `invite_events` bestaat mét index op `(user_id, created_at desc)` maar wordt door geen regel code geschreven of gelezen. CLAUDE.md beveiligingsregel 5 eist expliciet een limiet per gebruiker per dag. Hoe erg brute-force wordt hangt af van het codeformaat, en dat is nog niet geschreven — dit is dus het moment. Daarnaast: `on conflict do update set status = 'active'` laat een lid dat een admin op `inactive` zette zichzelf terugzetten (moderatie-bypass). Gedeeltelijk gedicht in 0008: 20 pogingen per gebruiker per dag, elke póging geteld (niet alleen de geslaagde), en de moderatie-bypass via `on conflict` is weg. **Nog open:** geen cap op groepen per gebruiker of leden per groep, en het codeformaat zelf is nog niet ontworpen (QS8-52).  ⚠️ **De "nog open"-lijst hierboven is op 25-08-2026 nagemeten en klopte op geen van de drie punten meer.** `join_group_with_code()` weigert bij twaalf actieve leden (`group_full`) en bij tien lidmaatschappen (`too_many_groups`); `create_group()` weigert bij tien groepen per dag én bij tien lidmaatschappen; en het codeformaat is ontworpen en gebouwd — `generate_invite_code()` plus `isCodeVorm()` (QS8-52, Done). ⚠️ **Dat een rij zichzelf "nog open" noemt, is dus geen bewijs.** Dit document beschrijft de stand op de dag van schrijven; alleen `pg_get_functiondef()` beschrijft de stand van vandaag. | ~~Middel~~ opgelost |
| 2026-08-16 | ✅ `goal_events_select` | ⚠️ **Productbeslissing, geen technische.** De policy geeft groepsgenoten leestoegang op `deadline_moved`, `scope_reduced` en `milestone_dropped`. Dat zijn per definitie tegenslagsignalen over iemand anders. `001-datamodel.md` §4.2 staat het toe ("als het doel"); domeinregel 7 verbiedt het. Die twee documenten spreken elkaar tegen en één moet wijken. Advies: domeinregel 7 laten winnen. Bijkomend: `goal_events_insert` geeft de doeleigenaar schrijfrecht met vrije jsonb terwijl §4.2 "systeem" belooft — een gebruiker kan zijn eigen audittrail vervalsen.  ✅ **Gesloten op 25-08-2026, migratie 0087 — en de productbeslissing was al genomen, elders.** `VERBODEN_GEBEURTENISSEN` in `chat-schemas.ts` noemt `milestone_dropped` met naam als iets dat nooit een systeembericht mag worden, terwijl `goal_events_select` het via een SELECT gewoon uitgaf. Dat maakte het een tegenspraak in plaats van een open vraag, en domeinregel 7 wint. `scope_reduced` en `milestone_dropped` zijn uit `goal_events_type_valid` gehaald; `deadline_moved` blijft, want dat is de benoemde verruiming A7 — je vraagt hem zelf aan en een buddy keurt hem goed. ⚠️ **Weghalen en niet afschermen, want niets schreef ze:** `logGoalEvent()` wordt drie keer aangeroepen (`created`, `archived`, `completed`) en `beslis_deadline_verzoek()` schrijft `deadline_moved`. Exact de vorm van 0082. ⚠️ **De tweede helft — de vervalsbare audittrail — is deels gesloten:** `goal_events_insert` laat een client alleen nog `created`, `archived` en `completed` schrijven, dus `deadline_moved` (de enige die een uitspraak over een ánder mens draagt) kan niet meer verzonnen worden. Een kale intrekking kon niet: `logGoalEvent()` is een echte cliëntschrijver. **Wordt zwaarder als:** er ooit een punt, een reeks of een groepszichtbaar oppervlak aan een `goal_events`-rij gaat hangen — dan is het geen dagboek meer maar een bron, en dan moet ook die helft dicht. | ~~Hoog~~ deels opgelost |
| 2026-08-16 | ✅ Views met een projectie die er niet is | De matrix belooft bij `breathers` "groepsleden zien alleen dát er een pauze loopt", maar de policy geeft de hele rij inclusief `starts_cycle` en `ends_cycle` — de lengte van een adempauze is een zwak tegenslagsignaal. Bij `profiles` krijgen groepsgenoten `reminder_time`, `reminder_enabled`, `reminder_tone` en `tz`; die eerste twee samen zijn iemands dagritme. Beide horen een view met projectie te zijn, zoals `group_visible_streaks`.  📏 **Nagemeten op 25-08-2026 en staat nog.** `breathers_select` is `user_id = auth.uid() or (… shares_group_with_goal(g.id))` — nog steeds de hele rij, dus `starts_cycle` en `ends_cycle` zijn voor groepsgenoten leesbaar. RLS kan geen kolommen beperken; hier is een view met een expliciete kolomlijst voor nodig.  ✅ **De profielhelft is gesloten op 25-08-2026, migratie 0089.** `authenticated` heeft geen tabelbrede SELECT meer op `profiles` maar een expliciete kolomlijst: `id`, `display_name`, `avatar_url`. `reminder_time`, `reminder_enabled` en `reminder_tone` zijn daarmee voor groepsgenoten dicht — en dat was geen tegenslagsignaal maar iemands dagritme, dat niemand ooit besloten heeft te delen. De eigenaar leest zijn eigen rij via de nieuwe view `mijn_profiel` (`security_invoker = false`, `security_barrier = true`), de notificatiejob leest ze als `service_role` en merkt er niets van. ⚠️ **Een nieuwe kolom op `profiles` is nu standaard dicht** tot iemand hem aan die lijst toevoegt — de goede kant om op te falen. ⚠️ **En de val die dit bijna een lege migratie maakte:** een `revoke select (kolom)` haalt níéts af van een grant die op de héle tabel staat. Postgres houdt het tabelrecht, de migratie draait zonder waarschuwing, en er verandert niets. Alleen de test die een groepsgenoot die kolommen liet opvragen liet het zien. Eerst tabelbreed intrekken, dán per kolom teruggeven. ✅ **En de `breathers`-helft is beslist op 25-08-2026 — besluit A50.** Het was geen lek maar een tegenspraak: deze rij citeerde de matrix ("alleen dát er een pauze loopt") terwijl `breathers_select` altijd de hele rij gaf en `adempauze.ts` dat óók zo beschrijft. Quinten heeft beslist welk document wijkt: **de matrix**. Aankondigen vooraf is de eigen handeling van de gebruiker, en dat is domeinregel 7's eigen uitzondering — een plan vooraf is iets anders dan een oordeel achteraf. ⚠️ **De eigenlijke bevinding lag ergens anders:** dit oppervlak stond helemáál niet in `002-domeinregel7-oppervlakken.md`, terwijl het sinds QS8-82 bestaat en de groep het leest. Daardoor kon de tegenspraak maanden blijven staan. Het staat er nu als **oppervlak 21**, in beide tabellen. ⚠️ Wat dicht blijft: welke weken in die periode gemist zijn (`weekly_goals.status`, sinds 0047, `excused` incluis). De groep ziet de pauze, niet de weken. Vastgelegd met een positieve controle in `tests/rls/epic8.test.ts`, met de hand rood gemaakt door de afscherming alsnog te bouwen. | ~~Middel~~ opgelost |
| 2026-08-16 | Migratie 0005 en `security_invoker` | Als 0005 gedraaid wordt, moet er méér in dan alleen `security_invoker = false`: (1) `security_barrier = true`, zodat `shares_group_with_goal()` gegarandeerd vóór de quals van de aanroeper draait; (2) `and s.user_id = g.owner_id` in de WHERE, want de autorisatie luidt nu "deelt de kijker een groep met dit doel" en niet "hoort deze reeks bij de eigenaar"; (3) een opmerking dat `force row level security` op `user_streaks` de view stilzwijgend breekt — de reparatie leunt volledig op de keuze in 0003 om FORCE níét te zetten, en dat is een onzichtbare koppeling tussen twee bestanden. Ná 0005 is de kolomlijst van de view de énige bescherming van `total_points`.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** `group_visible_streaks` draagt `security_invoker = false` **én** `security_barrier = true` — exact wat deze rij eiste. (`goal_dashboard` staat op `security_invoker = true`, en dat is voor die view de juiste stand.) | ~~Hoog~~ opgelost |
| 2026-08-16 | Supabase Storage | `completions.attachment_url` en `chat_messages.attachment_url` bestaan, maar er is geen enkele migratie die een bucket of een `storage.objects`-policy aanmaakt. Nu niet exploiteerbaar omdat er nog niets geüpload wordt; op de dag dat de eerste upload gebouwd wordt is het een open bucket tenzij iemand eraan denkt.  📏 **Nagemeten op 25-08-2026 en staat nog.** `storage.buckets` is leeg — nul buckets, dus ook nul policies. De twee `attachment_url`-kolommen wijzen nog steeds nergens heen. | Middel |
| 2026-08-16 | ✅ Tests tegen productie | De RLS-suite maakt echte accounts aan in het enige Supabase-project dat er is, en ruimt ze op met de service-role-key. Er staat nu een guard op (`RLS_TEST_ALLOW_PROD`), maar de echte oplossing is een lokale stack. Hangt samen met de regel van 15-08 over migraties op remote: dat is niet alleen een migratiekwestie.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Opgelost door de lokale opstelling (QS8-119). `RLS_DOEL=lokaal` draait tegen een database die het script zelf opbouwt en weggooit; het echte project wordt alleen nog geraakt door `token.test.ts`, dat bewijst dat GoTrue correcte claims uitgeeft — en dat kán niet lokaal. | ~~Middel~~ opgelost |
| 2026-08-16 | `user_streaks.current_streak` gedeeld | Het beslisdocument (§4.3) staat expliciet toe dat groepsgenoten `current_streak` zien, en QS8-55 vraagt er met naam om. Maar een reeks die terugvalt naar 0 is net zo goed zichtbaar bewijs van een gemiste week als een dalend puntentotaal — en domeinregel 7 noemt "verbroken reeks" letterlijk. **Sinds EPIC 5 staat het écht op het scherm**, dus dit is geen overweging meer maar een keuze die gemaakt moet worden. `best_streak` is er in 0019 al uit; `current_streak` is gebouwd zoals de issue hem vraagt. Alternatief: de groep alleen De Ketting tonen (`chain_links`, opdagen). Staat als A15 in `docs/Q-TODO.docx`.  ✅ **Nagemeten op 25-08-2026.** Dit is geen open vraag meer maar besluit **A15**, en het staat als zodanig in `CLAUDE.md`: de groep mag je reeks zien. Het is een van de twee benoemde verruimingen op domeinregel 7, met onderbouwing in `docs/decisions/002-…md` §4a. ⚠️ En sinds A41 varieert het bovendien op `groups.zichtbaarheid` — `best_streak` en `last_cycle_start` zijn in 0078 achter de open/beschermd-keuze gezet. | ~~Hoog~~ besloten (A15) |
| 2026-08-16 | CI draait de RLS-suite niet | De suite is inmiddels 63 tests en dekt de kern van EPIC 5: dat een buitenstaander niets van een groep ziet, dat de twintig-pogingenlimiet écht werkt, dat het puntentotaal en de beste reeks niet lekken, dat een gemiste week onzichtbaar is. Ze staan allemaal achter `rlsTestsConfigured`, en die env staat in GitHub uit — met goede reden, want de service-role-key hoort niet op een runner. Praktisch betekent het: **groen in GitHub zegt niets over EPIC 5.** Zolang er geen lokale stack is (besluit A9), is de enige dekking dat iemand er lokaal aan denkt. Zet dat vast in een ritueel of los het op met `supabase start` in CI.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** `.github/workflows/ci.yml` heeft een eigen `rls`-job die `scripts/lokale-stack.sh` opzet en `npm run rls:lokaal` draait, plus een `poort`-job die `needs: [controle, rls]` heeft. De commentaarregel erboven zegt het zelf: *"wat hier groen is, is bewezen; wat hier niet staat, is niet bewezen."* | ~~Hoog~~ opgelost |
| 2026-08-16 | Gedeelde bouwstenen staan dubbel | `Resultaat<T>`, `Pagina<T>` en de paginagrootte staan nu woordelijk in `modules/goals/api.ts` én `modules/buddies/api.ts`, inclusief dezelfde doc-comments. Omdat modules elkaars binnenkant niet mogen importeren, zijn het twee verschillende nominale types en importeert `app/groep/[id].tsx` er al één van elk. Bij `completions` en `commitments` worden het kopie 3 en 4. Hoort naar `src/shared/` vóór de volgende module. Zelfde verhaal voor het `let levend = true`-effect: dat staat nu negen keer woordelijk in `app/` en vraagt om één `useAsync(fn, deps)` die in `AsyncView` past.  📏 **Nagemeten op 25-08-2026 en staat nog.** `Pagina<T>` en `Resultaat<T>` staan nog in `modules/goals/api.ts` én `modules/buddies/api.ts`. | Middel |
| 2026-08-16 | `group_visible_streaks` in het hete pad | `group_overview()` doet een LEFT JOIN op een view met `security_barrier = true`, waarvan de WHERE `shares_group_with_goal()` aanroept — een `stable` functie die niet leakproof is. De barrier is een optimalisatiehek: onduidelijk of Postgres die functie per lid evalueert of per rij van `user_streaks × goals`. Bij 100k gebruikers is dat het verschil tussen twaalf en enkele honderdduizenden aanroepen op het meest bezochte scherm. `EXPLAIN ANALYZE` op een gevulde dataset geeft binnen tien minuten uitsluitsel; is het fout, dan wordt het een `left join lateral` beperkt tot `(m.user_id, d.id)`.  📏 **Nagemeten op 25-08-2026 en staat nog.** De view draagt nog steeds `security_barrier = true` met `shares_group_with_goal()` in de WHERE. ⚠️ **Wél gemeten in plaats van gevreesd:** `group_overview()` is warm 4,6–6,9 ms bij 50 leden en 16,9–18,7 ms bij 200. De kosten schalen met de groepsgrootte en niet met de paginagrootte. Voor een buddy-groep van drie tot acht mensen is dit niets. | Middel |
| 2026-08-16 | Regel 15 in de schermlaag | Elk scherm is 120–430 regels terwijl `CLAUDE.md` <50 regels per functie eist, en ESLint dwingt niets af (`eslint.config.js` heeft geen `max-lines-per-function`). De regel bestaat dus alleen op papier, en dat is het slechtste van beide werelden: elk scherm overtreedt de grondwet en niemand doet er iets mee. Kiezen: handhaven met een expliciete uitzondering voor `app/**`, of de regel in `CLAUDE.md` nuanceren tot iets dat wél geldt. **Wordt zwaarder als:** er een tweede lezer komt. Vanaf de engineer-review in november leest iemand deze schermen koud, en dan is "de regel staat op papier maar niemand handhaaft hem" niet meer uit te leggen. | Laag |
| 2026-08-16 | Repo en platform lopen uit elkaar | Twee keer op één dag gevonden, allebei bij toeval. (1) De migratie `grant_rollover_helpers_to_service_role` stond wél op het project maar niet in `supabase/migrations/` — gezien doordat `list_migrations` vijftien regels gaf en de map veertien bestanden. Nu gereconstrueerd als 0015. (2) De gedeployde `rollover`-functie had `/^Bearer\s+/i`, de repo `/^Bearers+/i` — de backslash was weggevallen bij een meerregelige zoek-en-vervang. Dat patroon matcht nooit een echte header, dus bij de eerstvolgende deploy vanuit de repo was de rollover stilgevallen met 403. **Er is geen enkele controle die repo en platform tegen elkaar houdt.** Zolang migraties via een MCP-tool gaan in plaats van via `supabase db push`, is de map een verslag en geen bron.  ✅ **Nagemeten op 25-08-2026.** Uitgelijnd en sindsdien bewaakt. `supabase_migrations.schema_migrations` bevat 90 rijen, genummerd `0001` t/m `0087` zonder één afwijkende versie, en `supabase/migrations/` heeft er evenveel. `npm run register:controle` legt beide kanten naast elkaar en draait mee in `/audit`; `npm run migraties:controle` bewaakt daarnaast dat er geen gat in de nummering valt — de belangrijkste van de twee, want een ontbrekend bestand laat de RLS-suite een ánder schema toetsen dan productie. | ~~Hoog~~ opgelost |
| 2026-08-15 | `package.json` | `npm audit` meldt 21 kwetsbaarheden in productie-deps, maar het zijn er in werkelijkheid drie: `image-size` (2× DoS) en `uuid` (buffer bounds). Allemaal transitief via `@expo/config-plugins` en alleen actief in **build-tooling** (prebuild), niet in de app zelf. `npm audit fix --force` zou Expo downgraden. Afwachten tot Expo bumpt; opnieuw controleren bij elke SDK-upgrade. **Wordt zwaarder als:** een van deze deps uit de build-tooling in de app-bundel belandt. Vandaag draait `image-size` alleen bij prebuild; in de bundel is het een DoS-pad dat gebruikers raakt. | Laag |
| 2026-08-15 | `shared/time` — DST-correctie | `utcFromZoned()` doet twee rondes om de offset te bepalen. Dat is de standaardtruc, maar het gedrag in het "gat" van de voorjaarsovergang (een lokale tijd die niet bestaat) is niet expliciet gespecificeerd — we landen op het eerstvolgende bestaande moment. Zolang cycli op middernacht beginnen speelt dit alleen in tijdzones die om middernacht wisselen. Laten nakijken. **Wordt zwaarder als:** een cyclus niet meer op middernacht begint, of als er een gebruiker in een tijdzone zit die om middernacht wisselt. Zolang beide niet gelden, is het gat onbereikbaar. | Laag |
| 2026-08-15 | Migratie op remote | Er is geen lokale Supabase-stack (geen Docker, geen CLI), dus 0001–0004 zijn rechtstreeks op het remote project gedraaid. Op een lege database is dat risicoloos, maar zodra er data in staat mag dit niet meer — `CLAUDE.md` verbiedt het. Lokale stack inrichten vóór migratie 0005.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Historisch feit over 0001–0004, en de situatie is sindsdien veranderd: er ís nu een lokale opstelling (`scripts/lokale-stack.sh`) en elke migratie wordt daar eerst afgespeeld. Blijft staan als context, niet als openstaand werk. | Middel |
| 2026-08-18 | `chain_links` wordt door niets geschreven | ✅ **Volledig opgelost, 19-08 en 24-08.** De twee schrijvers kwamen in migraties 0036 en 0037 (QS8-80); het systeembericht bij een ketting-mijlpaal kwam in 0070 (QS8-70, 24-08), waarmee de definitie die hier ontbrak is ingevuld: een rond cumulatief aantal schakels van de groep. Waarom cumulatief en niet "voltallig" of "N perioden op rij" staat in de kop van 0070 — die twee zijn conditioneel, dus het uitblijven van het bericht zou zelf het bericht worden. Was: Gevonden tijdens EPIC 7. `group_overview()` berekent `closed_this_period` met een `exists` op `chain_links`, en die tabel heeft geen enkele schrijver: niet de rollover-functie, niet de app, geen trigger. Het bolletje "deze week al afgesloten" op het groepsoverzicht staat dus altijd uit, en de Caption eronder legt een betekenis uit die niet bestaat. Het raakt ook EPIC 7 rechtstreeks: het acceptatiecriterium "ketting-mijlpaal" van QS8-70 is **niet gebouwd**, omdat De Ketting nog niet bestaat en de definitie van een mijlpaal erin bij QS8-80 hoort. Bij het bouwen van EPIC 8: dit is de eerste rij die er moet zijn, en dan komt `chain_milestone` op de allowlist in `chat_messages_system_event_bekend`. | ~~Hoog~~ opgelost |
| 2026-08-18 | Systeemberichten bevatten bewust geen titels | Bewuste keuze van EPIC 7, met kosten. Een systeembericht noemt de persoon en de gebeurtenis — "Sanne heeft een mijlpaal gehaald" — en nooit de doeltitel, weektitel, mijlpaaltitel, notitie of het gehaalde niveau. Reden: een bericht is een onveranderlijke kopie die de autorisatie overleeft waaronder hij gemaakt is. Koppelen is de toestemming (QS8-54) en ontkoppelen trekt hem in, maar wist geen chat; een chatbericht met de doeltitel erin blijft daarna staan. Dat is dezelfde klasse fout als de snapshots die policies overleefden in 0006, 0010, 0019 en 0023. **Wat het kost:** de chat is minder informatief en het kan als ruis gaan voelen. Ruimer maken is een productbeslissing, geen bugfix. Onderbouwing in `docs/decisions/002-domeinregel7-oppervlakken.md` §3; een test in `tests/rls/epic7.test.ts` houdt de keuze vast.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Een genomen besluit met bekende kosten (beslisdocument 002 §3). Dat is geen bevinding maar een afweging die je opnieuw kunt maken. | Middel |
| 2026-08-18 | Geen limiet op `chat_messages` en `week_review_replies` | Beide tabellen zijn rechtstreeks beschrijfbaar via PostgREST zonder enige rate limit. De praktische rem is sociaal (maximaal twaalf leden per groep, en het zijn mensen die je zelf hebt uitgenodigd) en de bovengrens per bericht is 4000 respectievelijk 1000 tekens. Op een gratis tier van 500 MB is een lid dat in een lus schrijft toch een kostenvector — en `wek_groep()` doet er per bericht een `update groups` bovenop. CLAUDE.md beveiligingsregel 5 noemt AI-calls, uitnodigingen en auth met naam en de chat niet, dus dit is bewust buiten EPIC 7 gelaten. Bij het inrichten van limieten: dit is een `insert`-pad, dus een trigger die weigert kan hier wél gooien — de valkuil van 0017 gaat over SECURITY DEFINER-RPC's die iets willen ónthouden.  ✅ **Gedicht in 0090 op 25-08-2026.** Vijfhonderd chatberichten en honderd weekreacties per voortschrijdend etmaal, per persoon, afgedwongen in de INSERT-policy. ⚠️ **In de policy en niet in een trigger, tegen wat deze rij zelf voorstelde.** De rij had gelijk dat een trigger hier mág gooien en een leesbare melding oplevert — maar systeemberichten komen uit definer-functies en de rollover draait als `service_role`, en die vallen vanzelf buiten een policy die `to authenticated` staat. Een trigger zou zélf op een rolnaam moeten beslissen, en zo'n trigger faalt open (WERKVOORRAAD §7). ⚠️ **De leesbare melding is anders opgelost, en dat is het interessante deel.** `berichten_over()` en `weekreacties_over()` geven het *resterende* budget terug in plaats van het verbruik, zodat het getal alleen in de migratie staat; de app roept dezelfde functie aan op het foutpad om te zeggen wát er misging. 0083 telde vooruit en zette het getal in de policy — dan kan de app het niet kennen, en is elke 42501 dezelfde. ⚠️ **En passant:** er stond geen énkele index op `chat_messages.sender_id` terwijl drie policies erop filteren — onwrikbare regel 11 gold daar niet. Bewezen in `tests/rls/rem.test.ts` (zes gevallen, waaronder dat de rem per persoon geldt en niet per groep, en dat de teller zonder sessie nul zegt in plaats van de hele limiet) en met de hand rood gemaakt door de clausule lokaal uit de policy te halen: de rij landt dan wél. | ~~Middel~~ opgelost |
| 2026-08-18 | `week_reviews.group_period_start` komt van de client | Bewust, en om de juiste reden: de groepsklok hoort in `shared/time` en de database mag er niet mee rekenen (correctheidsregel 7) — dezelfde afspraak als bij `group_overview()`. Het gevolg is wel dat een lid zijn eigen weekafsluiting in een willekeurige periode kan zetten, ook een toekomstige of een van vijftig weken terug. Vandaag onschadelijk: het gaat om een eigen rij, er hangen geen punten aan, en de app vraagt alleen de lopende periode op. Het wordt onnetjes zodra er een geschiedenis van weekafsluitingen komt. Zelfde categorie als de rij hierboven over `p_period_start` in `group_overview()`, en dezelfde oplossing: server-side klemmen op lopende en vorige periode, of de vrijheid expliciet vastleggen. **Wordt zwaarder als:** de groep die periode ergens te zien krijgt, of zodra er iets op geboekt wordt (punten, De Ketting, een mijlpaal). Dan is een zelfgekozen periode geen rariteit meer maar een hefboom. | Laag |
| 2026-08-18 | Vraag 1 van de weekafsluiting komt uit de Dagzetten | De Dagzet is standaard privé (domeinregel 9); de weekafsluiting is dat niet. `voorstelUitDagzetten()` vult vraag 1 voor met de eigen Dagzetten van de groepsperiode, in een veld dat de gebruiker zelf moet bevestigen, met een hint die zegt waar de tekst vandaan komt. De bescherming is dus **uitsluitend** dat iemand die hint leest voordat hij op "Delen met mijn groep" drukt. Laten nakijken of dat genoeg is, of dat het voorstel achter een expliciete knop hoort ("vul in uit mijn Dagzetten") in plaats van standaard in het veld. Dit is de enige plek in de app waar privé tekst in een niet-privé veld terechtkomt zonder tweede handeling.  📏 **Nagemeten op 25-08-2026 en staat nog.** `voorstelUitDagzetten()` staat er nog en doet nog steeds hetzelfde. Het blijft een productafweging: de voorinvulling is een gemak, en de gebruiker kan hem overschrijven — maar niemand heeft hem verteld waar de tekst vandaan komt. | Middel |
| 2026-08-18 | EPIC 7 raakte 25 bestanden in één branch | CLAUDE.md noemt meer dan vijftien bestanden in één keer een moment om te overleggen. Dat is hier gebeurd omdat de afspraak voor deze sessie één branch per epic is en de epic vier issues omvat, waarvan één (7.6) per definitie de hele app raakt. Ter kennisgeving, niet als vrijbrief: bij EPIC 8 en verder is de vraag of de epic-branch nog steeds de juiste eenheid is, of dat de commit binnen de branch fijnmaziger moet. **Beantwoord op 23-08-2026: nee.** De eenheid is sindsdien één branch per Linear-issue, vastgelegd in `CLAUDE.md`. Aanleiding was PR #1 — acht issues op één branch met een zelfbedachte naam, waardoor Linear branch, PR en issue niet aan elkaar koppelde en alle acht statussen met de hand bijgewerkt moesten worden. **Wordt zwaarder als:** de branchindeling opnieuw ter discussie staat. ⚠️ **Beantwoord op 23-08:** één branch per Linear-issue, vastgelegd in `CLAUDE.md`. Deze rij is daarmee afgehandeld. | Laag |
| 2026-08-18 | Onopgeslagen tekst in de weekafsluiting | Bevinding van de gebruikersreview op EPIC 7, gedeeltelijk opgelost. Wie de drie vragen invult en dan weg navigeert, is zijn tekst kwijt — inclusief vraag 2, de enige plek in de app waar iemand zijn eigen tegenslag opschrijft, en precies de tekst die je niet nog een keer wil typen. De uitgang binnen de app is nu gedekt: staat er onopgeslagen tekst, dan wordt "Terug naar de groep" eerst een waarschuwing plus "Toch weg, zonder delen". **Wat níet gedekt is:** de terugknop van de browser, verversen, de hardwareknop op Android, en het wisselen van tab in de router. Dat vraagt een `beforeunload`-equivalent per platform of een lokaal concept, en een concept van vraag 2 in AsyncStorage is zelf een privacyvraag (het staat dan onversleuteld op het toestel). Kiezen, niet vergeten.  📏 **Nagemeten op 25-08-2026 en staat nog.** Er is nog geen conceptopslag voor de weekafsluiting; de enige `AsyncStorage`-gebruikers in `modules/buddies` zijn de uitnodigingscode en de chat. | Middel |
| 2026-08-18 | Terugnemen van een weekafsluiting wist andermans reacties | Bevinding van de gebruikersreview op EPIC 7, opgelost. `week_reviews` cascadeert naar `week_review_replies`, dus "Terugnemen" haalde niet alleen je eigen tekst weg maar ook de aanmoediging die je buddy eronder zette — met één tik, zonder bevestiging, en zonder dat zij het wist. In een groep van drie is dat precies het soort moment dat vertrouwen kost. Er staat nu een tweestap met het aantal reacties erin. De cascade zelf blijft: een reactie op een antwoord dat niet meer bestaat is een halve zin over iets dat niemand kan nalezen. Een `Alert` was hier niet bruikbaar — die is op react-native-web niet betrouwbaar. | ~~Middel~~ opgelost |
| 2026-08-18 | Systeemberichten voelen als ruis | De gebruikersreview op EPIC 7 bevestigt de spanning die Q-TODO A22 beschrijft: "Sanne heeft een mijlpaal gehaald" zonder te zeggen welke mijlpaal is niet iets om op te reageren. De keuze om geen titels in een systeembericht te zetten is technisch goed onderbouwd (beslisdocument 002 §3: een bericht overleeft de autorisatie waaronder hij gemaakt is) maar kost product. Twee kanten van dezelfde beslissing, en het is een productkeuze. Als hij anders uitvalt: de test in `tests/rls/epic7.test.ts` die de titels nergens in een systeembericht mag terugvinden, moet dan mee veranderen.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Toon en frequentie beoordeel je met gebruikers, niet met een query. | Middel |
| 2026-08-18 | De huddledag valt op de dag zelf niet op | Bevinding van de gebruikersreview op EPIC 7. Het groepsscherm toont "HUDDLEDAG ZONDAG" als vaste tekst, en de weekafsluiting is één tik ver — maar er is niets dat op de dag zelf opvalt. Voor een ritueel dat één keer per week gebeurt, hangt de opkomst daarmee aan de vraag of iemand het onthoudt. Dat is hetzelfde patroon als "beoordelen is alleen zichtbaar als je ernaar zoekt" (17-08) en het hoort bij EPIC 11: dit is de tweede melding die er moet zijn, na de melding dat er iets te beoordelen valt.  🗣 **Beoordeeld op 25-08-2026: niet te meten.** Ontwerpvraag uit de gebruikersreview; er is geen stand die dit bevestigt of ontkracht. | Middel |
| 2026-08-18 | Offset-paginering op reacties kan er één overslaan | Bevinding van de code-review op EPIC 7. `weekafsluiting_reacties()` pagineert met `limit`/`offset` op `(created_at, id)` oplopend. Voor tóevoegingen is dat stabiel — nieuwe rijen krijgen de hoogste tijdstempel en komen achteraan — maar `verwijderReactie()` bestaat, en een verwijdering vóór de cursor schuift alles één plek naar voren. Dan slaat "Meer reacties laden" precies de rij over die net de grens overschreed. Alleen relevant bij meer dan honderd reacties in één periode. `voegReactiesSamen()` ontdubbelt wel, maar kan een overgeslagen rij niet terughalen. Oplossing is een cursor op `(created_at, id)`, zoals de chat die al heeft. **Wordt zwaarder als:** een reactielijst langer wordt dan één pagina én verwijderen gebruikelijk is. Vandaag is geen van beide waar. | Laag |
| 2026-08-18 | Gedeelde patronen in de schermlaag staan nog los | Bevinding van de code-review op EPIC 7. Drie dingen staan nu meer dan één keer: het `let levend = true`-laadeffect (inmiddels in elf schermen), het mapped-type met nullability op een RPC-rijtype (`ChatRij`, `AntwoordRij`, `ReactieRij`, `OverzichtRij`, `WachtrijRij`), en `gevalideerd.error.issues[0]?.message ?? '…'`. Het samenvoegen-en-ontdubbelen is in deze ronde wél naar `modules/buddies/merge.ts` gegaan, want die kopie stond in `app/` en dat is domeinlogica in een scherm. De rest hoort naar één `useAsync(fn, deps)`, één `Nullable<T>` en één `eersteFout(...)`. Elk nieuw async-scherm kopieert het anders opnieuw, en er komen nog zes epics.  📏 **Nagemeten op 25-08-2026 en staat nog.** ⚠️ **En het is erger geworden, niet beter:** het `let levend = true`-laadeffect staat nu in **achttien** bestanden, tegen elf toen deze rij geschreven werd. `AsyncView` bestaat wel en dekt de weergavekant; het effect eromheen is nog nergens gedeeld. | Middel |
| 2026-08-18 | ✅ De aanmeldlimiet van Supabase maakt de RLS-suite vlokkerig | `policies.test.ts` en `epic7.test.ts` maken samen zo'n tien echte accounts per volledige run, elk met een `signInWithPassword`. Supabase weigert dat na ongeveer dertig aanmeldingen per uur met "Request rate limit reached", en dan valt de suite om op een plek die niets met de policies te maken heeft — twee keer op één avond gezien, beide keren in een ander bestand. Zolang er geen lokale stack is (A9) is dit een harde bovengrens op hoe vaak je de suite achter elkaar kunt draaien. Structurele oplossing: één set gedeelde testgebruikers over beide bestanden, of de sessie hergebruiken in plaats van per gebruiker opnieuw inloggen.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Zelfde reparatie als de rij hierboven: sinds QS8-119 draait de suite lokaal en raakt hij de aanmeldlimiet niet meer. | ~~Middel~~ opgelost |
| 2026-08-19 | `ketting_schakel()` vertrouwt op twee data van de client | QS8-80. Correctheidsregel 7 verbiedt SQL de groepsperiode uit te rekenen, dus de RPC krijgt `p_period_start` (groepsperiode) én `p_cycle_start` (persoonlijke cyclus) van de client, allebei uit `shared/time`. Hij controleert lidmaatschap en het bestaan van een goedgekeurd weekdoel in die cyclus, maar **niet of de twee data bij elkaar horen** — daarvoor zou hij moeten rekenen. Een lid kan dus in theorie één goedgekeurd weekdoel gebruiken om een schakel in een andere periode binnen het venster van vijf weken te zetten. Grens en niet nul: de bovengrens sluit de toekomst af, de ondergrens de geschiedenis. Weegt licht omdat een schakel geen punten geeft en de teller op groepsniveau blijft, maar het is de eerste plek waar de database een regel niet volledig afdwingt. Sluitend maken kan alleen door de groepsperiode in SQL te kunnen afleiden — dat vraagt de huddledag en de tijdzone in de database, en dat is precies wat 0016 verbiedt. Bespreken.  ✅ **Nagemeten op 25-08-2026.** Beide parameters worden nu getoetst in plaats van geloofd, en de functie is `SECURITY DEFINER`. Correctheidsregel 7 blijft de reden dat de dátums van de client komen — het verschil is dat de server ze nu controleert. | ~~Middel~~ opgelost |
| 2026-08-19 | De Ketting maakt de aanwezigheidsmatrix uitleesbaar | Gevonden door alle drie de reviewers, onafhankelijk, alle drie "blokkerend". `chain_links_select` is `using (is_group_member(group_id))` zonder periodegrens. Zolang de tabel leeg was, was dat theorie; migratie 0036 vult hem. Eén verzoek buiten de UI om geeft per lid de lijst periodes waarin hij opdaagde, en het complement daarvan is de lijst gemiste weken, met naam en datum. `group_overview()` levert hetzelfde netter: `p_period_start` is een vrije parameter, dus een lus over vijftig weken geeft het volledige raster. **Dit is exact het `weekly_goals_select`-patroon van EPIC 5, opnieuw.** **Gedicht in 0037** (akkoord Quinten 19-08-2026): rijbeperking op de policy en een venster in `group_overview()`, met tests die de aanval echt uitvoeren. Blijft hier staan als les, niet als openstaand punt: het gevaarlijke moment was niet het schrijven van de policy maar het vullen van een tabel waarvan de leeskant nooit voor data ontworpen was. **Vraag bij elke tabel die van leeg naar gevuld gaat: wat betekent een ontbrekende rij nu?** | ~~Hoog~~ opgelost |
| 2026-08-19 | Route 1 van De Ketting was te vervalsen | `week_reviews.group_period_start` heeft geen CHECK en `week_reviews_write` is `for all` op je eigen rijen. Invoegen met een zelfgekozen datum → schakel → eigen rij verwijderen → herhalen gaf een sluitende ketting over twee jaar in een halve minuut, en schakels zijn append-only. De kop van 0036 beweerde dat deze route juist de exacte was; dat was fout. **Gedicht in 0037**: een BEFORE-trigger op `week_reviews` weigert de rij zelf. Bewust geen CHECK: PostgreSQL accepteert `current_date` daarin wél, maar zo'n constraint is niet immutable en breekt een restore van een oudere dump — en de dump is op de gratis tier het enige vangnet. | ~~Hoog~~ opgelost |
| 2026-08-19 | ✅ `ketting_stand()` gaf twee leden een verschillend getal | Vondst van code-critic. De functie was SECURITY INVOKER en telt leden zonder lopende adempauze; `breathers_select` toont andermans pauze alleen als het doel een groep met je deelt. Op een teller die per definitie gedeeld is, zag daardoor niet iedereen hetzelfde. Gerepareerd in 0037 (SECURITY DEFINER met expliciete lidmaatschapstoets). De les is algemener dan deze functie: **SECURITY INVOKER op iets dat een groepswaarheid teruggeeft, geeft per aanroeper een andere waarheid.**  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** `ketting_stand()` is nu `SECURITY DEFINER`, dus hij telt niet meer door de bril van de kijker en twee leden zien hetzelfde getal. | ~~Middel~~ opgelost |
| 2026-08-19 | Eén goedgekeurd weekdoel leverde tot 36 schakels op | `p_cycle_start` had geen enkele grens en `p_period_start` hoeft niet op een periodegrens te liggen, dus binnen het venster van vijf weken was elke datum een aparte schakel — en een cyclus uit maart werkte in augustus nog. 0037 eist nu dat de opgegeven periode binnen een week van de opgegeven cyclus ligt. Volledig sluitend wordt het pas met een kolom op `chain_links` die vastlegt welke cyclus de schakel verdiend heeft; dat is een datamodelwijziging en ligt bij Quinten.  ✅ **Nagemeten op 25-08-2026.** `ketting_schakel()` grenst `p_cycle_start` af; de functie weigert een cyclusdatum buiten de toegestane band. | ~~Middel~~ opgelost |
| 2026-08-19 | `current_date` als bovengrens breekt elke periodestart in de nacht | `current_date` is de serverdatum in UTC, `groupPeriod()` rekent in de tijdzone van de groep. In Europe/Amsterdam is een geldige periodestart tussen 00:00 en 02:00 lokale tijd één dag "in de toekomst"; in Pacific/Auckland twaalf uur lang. De ketting weigerde dan geldige aanroepen. Gerepareerd in 0037 met `current_date + 1`. Het is het middernachtprobleem uit domeinregel 2, in een grenscontrole in plaats van in een berekening — en dat is precies waar niemand het zoekt.  📏 **Nagemeten op 25-08-2026 en staat nog.** `ketting_schakel()` gebruikt nog steeds `current_date` — de serverdatum in UTC — als bovengrens. | Middel |
| 2026-08-19 | `breathers.goal_id` is een foreign key zonder index | Onwrikbare regel 11. `ketting_stand()` is de eerste die er per lid overheen loopt, en deed dat in 0036 twee keer door een gedupliceerde subquery. 0037 maakt er één CTE van; de index ontbreekt nog. Bij 100k gebruikers is dit de hete plek van deze feature. **Wordt zwaarder als:** `breathers` gevuld raakt. De tabel staat op nul rijen, dus de ontbrekende index kost vandaag niets — en dat is precies de valkuil "een redenering die klopt zolang een tabel leeg is". | Laag |
| 2026-08-19 | De Ketting belooft anonimiteit, de ledenlijst eronder geeft hem weg | Bevinding van de gebruikersreview op QS8-80, en de scherpste van die ronde. `Ketting.tsx` toont bewust aantallen en nooit namen; twintig pixels lager toont `MemberRow` per lid een naam met een gevuld of leeg bolletje voor exact dezelfde week. In een groep van twee of drie is "1 schakel deze week" daarmee geen anonimiteit maar een rekensom die het scherm zelf al voor je heeft opgelost. **Geen datalek** — het bolletje is een bewuste keuze uit QS8-55, onderbouwd met "binnen de lopende periode betekent leeg alleen nog niet" — maar wel een ontwerpinconsistentie: de zorgvuldigheid van het ene component wordt door zijn buurman ongedaan gemaakt. De klasse is nieuw en belangrijk: **domeinregel 7 wordt hier per component nageleefd en niet per scherm**, en geen enkele RLS-test kan dat vangen, want er lekt niets uit de database. Drie richtingen: het bolletje uit `MemberRow` halen (kost een acceptatiecriterium van QS8-55), De Ketting accepteren als samenvatting van iets dat toch al per persoon zichtbaar is (dan is de anonimiteit van de teller een vorm zonder inhoud), of de per-persoon-weergave bewust houden en dat in beslisdocument 002 vastleggen als vierde verruiming. Productbeslissing.  📏 **Nagemeten op 25-08-2026 en staat nog.** Het is een ontwerpinconsistentie tussen twee componenten en geen datalek — de rij zegt dat zelf al. Er is geen schemastand die dit oplost of bevestigt; het is de vraag of `MemberRow` naast `Ketting` mag staan, en dat is een ontwerpbesluit. | **Hoog** |
| 2026-08-19 | Twee voortgangsbalken, twee kopieën van dezelfde presentatielogica | Bevinding van de code-review op QS8-80. `Ketting.tsx` en `MilestoneProgress.tsx` bevatten woordelijk dezelfde reduced-motion-uitdrukking (`reduced || Platform.OS !== 'web' ? {} : { transitionDuration: '260ms' }`) en dezelfde `spoor`/`vulling`-structuur. Dit is géén domeinlogica die uit elkaar moet blijven — `kettingVulling()` en `milestoneProgress()` horen wél apart, want de eerste telt opdagen en de tweede voortgang — maar de balk zelf is opmaak die twee keer is overgetypt. Bij de derde balk (en die komt: reeks en punten op het dashboard, QS8-75) is het drie keer. Kandidaat: één `ProgressTrack`-primitive of een `useReducedMotionStyle()`-hook, zonder de scheiding in `metrics.ts` aan te tasten. **Wordt zwaarder als:** er een derde kopie bij komt. Bij twee is het duplicatie, bij drie is het een patroon dat niemand meer terugdraait. | Laag |
| 2026-08-19 | `weekpas_stand()` en de NULL-val in een autorisatietoets | ✅ **Gedicht in 0040, en het is de leerzaamste vondst van QS8-81.** 0039 begon met `if eigenaar is null or eigenaar <> auth.uid() then return null`. Is `auth.uid()` null, dan is `eigenaar <> auth.uid()` niet `false` maar `null`; de hele voorwaarde wordt `false or null` = `null`, de tak gaat niet af, en een SECURITY DEFINER-functie geeft daarna de weekpasvoorraad van een wíllekeurig doel terug — en een verbruikte pas is bewijs van een gemiste week (domeinregel 7). Gevonden met een proefaanroep als `service_role`, waar `auth.uid()` null is; in de praktijk stond `anon` nog buiten de deur via de grants, maar een JWT met rol `authenticated` zonder `sub`-claim komt er wél doorheen. **De regel die eruit volgt is algemener dan deze functie: `x <> y` is in SQL geen bewering over ongelijkheid zodra één kant leeg kan zijn, maar een derde antwoord dat zich als "niet waar" gedraagt.** De vijf oudere RPC's (`zet_doelstatus`, `zet_streefdatum`, `vraag_deadline_verschuiving`, `trek_goedkeuring_in`, `trek_deadline_verzoek_in`) doen het al goed met een expliciete `if auth.uid() is null`-tak — dit was geen nieuwe regel maar een bestaande conventie die één migratie niet volgde. **Laten nakijken of dit als test af te dwingen is** in plaats van als gewoonte: elke SECURITY DEFINER-functie die `auth.uid()` noemt, moet een null-tak hebben. | ~~Hoog~~ opgelost, patroon blijft |
| 2026-08-19 | ✅ `herbereken_reeks()` groepeert op (cyclus, status) en niet op cyclus | De lus draait over `select cycle_start_date, status ... group by cycle_start_date, status order by cycle_start_date`. Een cyclus met twee weekdoelen in verschillende toestanden — één `approved`, één `missed` — levert daardoor **twee rijen voor dezelfde week** op, en de volgorde tussen die twee is niet vastgelegd: `order by` noemt alleen de datum. Komt `approved` eerst, dan telt de week mee en breekt `missed` de reeks alsnog; andersom breekt hij eerst en telt hij daarna weer op vanaf één. Dezelfde data kan dus twee antwoorden geven, en welke je krijgt hangt af van het queryplan. Bestond al vóór QS8-81, maar wordt door weekpassen zichtbaarder: `verbruik_weekpas()` weigert nu expliciet een pas op een cyclus die al een `approved` weekdoel heeft, juist om te voorkomen dat er een schaarse pas verdwijnt aan een week die toch al telt — die weigering is een pleister op dit gat en geen reparatie. Juiste vorm is één rij per cyclus met een afgeleide status (telt de week mee: ja/nee), maar dat is een herschrijving van de reeksberekening en dat verdient een bewuste keuze.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Gedicht in 0074 (besluit A37): `pg_get_functiondef()` toont geen `group by … status` meer. ⚠️ Er staat hierónder nog een rij over hetzelfde, verhoogd naar Hoog — die is de leidende; deze is het eerste signaal. | ~~Middel~~ opgelost |
| 2026-08-19 | Een ontbrekende policy weigert stil, niet luid | Opgemerkt tijdens QS8-81. `week_pass_events`, `points_ledger`, `user_streaks` en `chain_links` hebben alle vier bewust alléén een SELECT-policy, maar dragen wél de standaard Supabase-tabelgrants (INSERT, UPDATE, DELETE voor `anon` en `authenticated`). Voor INSERT levert dat een harde `42501` op — er is geen rij om weg te filteren. Voor UPDATE en DELETE niet: RLS filtert de rijen weg, en een DELETE die nul rijen raakt is geen fout. De client krijgt dus HTTP 204 en een ongewijzigde tabel. **De gegevens zijn veilig** — dat is getest — maar het faalgedrag is stil, en een test die op `42501` rekent wordt groen zonder iets te bewijzen. Twee dingen om te overwegen: de tests op deze tabellen toetsen daarom de úítkomst (staat de rij er nog?) en niet de foutcode, en de schrijfrechten intrekken op tabellen die per ontwerp alleen door `service_role` geschreven worden zou de weigering luid maken. Dat laatste raakt vier tabellen tegelijk en is dus geen onderdeel van deze issue. **Wordt zwaarder als:** een van deze vier tabellen een UPDATE- of DELETE-pad krijgt. Nu weigert het stil op INSERT met een harde 42501; bij UPDATE filtert RLS de rijen weg en krijgt de client een 204 zonder dat er iets gebeurd is. | Laag |
| 2026-08-19 | De melding "een weekpas heeft je reeks gered" hangt aan het openen van de app | QS8-81 vraagt om "een melding achteraf dat hij je reeks gered heeft". Wat er gebouwd is, is een privé blok op het dashboard dat verschijnt zolang de zojuist afgesloten cyclus dezelfde is als de laatst geredde. Dat is de enige juiste plek — een systeembericht zou een gemiste week in de groepschat zetten — maar het betekent ook dat iemand die de app die week niet opent, nooit te horen krijgt dat zijn pas verbruikt is. Bij EPIC 11 (QS8-91) hoort dit een persoonlijke push te worden. Let op: die notificatie is **strikt persoonlijk** en mag in geen enkele groepsmelding terechtkomen. **Wordt zwaarder als:** push echt bezorgt. ⚠️ **Dat komt nu dichtbij** — QS8-124 heeft de registratie gebouwd. Zodra er meldingen aankomen, is "je ziet het pas als je de app opent" een keuze en niet langer de enige mogelijkheid. | Laag |
| 2026-08-19 | De maximale weekpasvoorraad is een getal dat ik zelf gekozen heb | QS8-81 zegt "met een maximum voorraad" en noemt geen getal. Het staat nu op **2**, als `weekpas_maximum()` in de database, met de redenering: zes voltooide cycli per pas plus een voorraad van twee betekent dat je twee weken kunt missen zonder je reeks te verliezen. Hoger maakt de pas waardeloos (missen wordt kosteloos en de reeks meet niets meer), lager maakt hem tot een fooi. Er zit geen onderzoek achter en geen gebruikersdata. Het is één regel SQL om te veranderen en er is bewust géén kopie van dat getal in TypeScript, dus aanpassen kan zonder release. Staat ook als A32 in `docs/Q-TODO.docx`. **Wordt zwaarder als:** er echte gebruikers zijn en het gedrag meetbaar wordt. Twee is nu een beredeneerde gok; met data is het een keuze die je kunt toetsen. | Laag |
| 2026-08-19 | ⚠️ `weekly_goals` mag door de eigenaar zélf op `approved` worden ingevoegd | **Gevonden door de security-review op QS8-81, en het is de terugkeer van de bevinding van 17-08 die in dit document als opgelost staat.** 0023 heeft `update` op `weekly_goals` ingetrokken en vier kolommen teruggegeven; **INSERT is nooit beperkt**. `weekly_goals_write` is `for all` met alleen een eigenaarstoets en de CHECK `weekly_goals_status_valid` staat `'approved'` gewoon toe. Zes keer `POST /rest/v1/weekly_goals` met `status:"approved"` op zes verschillende maandagen levert dus: een reeks van zes die de groep via `group_visible_streaks` te zien krijgt, én twee gratis weekpassen, want `verdien_weekpassen()` telt goedgekeurde cycli. Geen buddy nodig, geen goedkeuring, één verzoek per week. **De les die 0023 zelf opschreef — "een autorisatiegrens is pas dicht als ook het gevólg ervan op slot zit" — is toegepast op wijzigen en niet op aanmaken.** Reparatie is dezelfde vorm als 0019 §4 en 0023 §1: `revoke insert on weekly_goals from authenticated` plus een kolomlijst zonder `status`. ⚠️ Let op valkuil 17: zoek daarna elke insert op die kolom in `src/`, `app/` én `tests/`. ✅ **GEDICHT in 0043 (19-08), na besluit van Quinten** (A35). `revoke insert on weekly_goals from authenticated` plus een kolomlijst zonder `status`, en de `for all`-policy uit elkaar in een aparte insert-, update- en delete-policy — binnen één `for all` is DELETE niet strenger te maken dan INSERT. De UPDATE-policy blijft nodig voor de vier kolommen die 0023 teruggaf. Twee tests: de insert mét status geeft nu `42501`, en de insert zónder status (precies wat `maakWeekdoel()` doet) werkt en komt op `todo`. Die tweede staat er om valkuil 17 te vangen, want een ingetrokken kolomrecht breekt de app stil terwijl typecheck en lint groen blijven. ⚠️ Bewust níét ingetrokken: `points_ceiling`, `points_floor` en `points_miss`. Die worden begrensd door de CHECK uit 0007, en er staat een test op die een plafond van 100.000 probeert — die hoort op die CHECK af te gaan en niet op een grant, anders bewijst hij iets anders dan hij zegt. | ~~Kritiek~~ opgelost |
| 2026-08-19 | ⚠️ Een gemiste week is met één DELETE weg te poetsen | Zelfde ronde, zelfde policy. `weekly_goals_write` dekt ook DELETE, dus wie zijn reeks kwijt is doet `DELETE /rest/v1/weekly_goals?id=eq.<de gemiste week>`. `herbereken_reeks()` loopt over de rijen die er zíjn; zonder rij bestaat de onderbreking niet en loopt de reeks ongebroken door — zonder pas, zonder bovengrens, zo vaak als je wilt. Het minpunt blijft wél staan (`points_ledger.ref_id` heeft geen foreign key), dus domeinregel 10 houdt stand. Maar het maakt de hele economie van QS8-81 zinloos: een schaarse munt die een reeks beschermt die gratis te repareren is, is geen munt. Het is bovendien domeinregel 6: corrigeren gebeurt met een correctie-record, niet door geschiedenis weg te gooien. ✅ **GEDICHT — maar in drie migraties, niet in één, en dat is de les.** 0043 sloot de directe route; de review daarop vond twee ándere routes naar exact hetzelfde effect (**A39** doorschuiven, **A40** de `todo`-rij wissen vóór de rollover) en die zijn dezelfde dag gedicht in 0045. Zie de aparte regels hieronder. Deze regel stond een halve dag als "half gedicht" en dat was terecht: 0043 alleen was niet genoeg. Wat 0043 zelf sloot: `weekly_goals_delete` staat alleen nog toe wat nog `todo` is, dus zodra een week is ingediend, beoordeeld, gemist, vrijgesteld of meegenomen, is hij geschiedenis. ⚠️ De weigering is **stil** en niet luid — RLS filtert de rij weg en een DELETE die niets raakt is geen fout, dus de client krijgt HTTP 204 en een ongewijzigde tabel. De test toetst daarom de úítkomst (staat de rij er nog?) en niet de foutcode; zelfde reden als bij `week_pass_events`. Er staat ook een test op de andere kant: een weekdoel dat nog `todo` is moet je wél gewoon kunnen weggooien. `verwijderWeekdoel()` heet sinds 0045 `sluitWeekdoelAf()` en verwijdert niets meer. | ~~Kritiek~~ opgelost |
| 2026-08-19 | `herbereken_reeks()` groepeert op (cyclus, status) — **verhoogd naar Hoog** | Stond hierboven al als Middel; twee reviewrondes zeggen onafhankelijk van elkaar dat dat te laag is, en ze hebben gelijk. Ten eerste is de situatie niet exotisch: er is geen unieke constraint op `(goal_id, cycle_start_date)` en `maakWeekdoel()` staat zoveel weekdoelen per week toe als je wilt, dus twee weekdoelen op één doel in één week waarvan er één afkomt is een doodgewone dinsdag. Ten tweede is het gat sinds QS8-81 **dragend**: `verbruik_weekpas()` weigert een pas op precies zo'n cyclus, met als argument dat de `approved`-tak de reeks al in de lucht houdt — en die aanname klopt maar in de helft van de gevallen, want de sorteervolgorde tussen de twee rijen ligt niet vast. Netto kan een gebruiker een weekdoel hálen, tóch zijn reeks verliezen, en de pas die dat had opgevangen geweigerd zien. Reparatie is één rij per cyclus met een afgeleide status (`bool_or(status='approved')`), maar dat is materieel de reeksberekening wijzigen en dat vraagt volgens CLAUDE.md eerst Quinten. De vraag die beantwoord moet worden is klein: **telt een week mee zodra er één weekdoel in af is, ja of nee?** Staat als **A37** in `docs/Q-TODO.docx`.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Gedicht in 0074 (besluit A37): `pg_get_functiondef()` toont geen `group by … status` meer, en de reeks is naar de weekpas toe getrokken in plaats van andersom. | ~~Hoog~~ opgelost |
| 2026-08-19 | `profiles.tz` is vrije tekst en kon de hele rollover stilleggen | Gevonden door de security-review op QS8-81. `profiles.tz` heeft geen CHECK en de eigenaar mag hem zelf zetten; `Intl.DateTimeFormat` gooit een `RangeError` op een onbekende zone. De `for`-lus in de rollover had daar geen `try` omheen, dus één gebruiker met een typefout legde de hele job om — elk uur opnieuw, op hetzelfde profiel, zodat niemand ná hem ooit aan de beurt kwam. Geen week gesloten, geen minpunt geboekt, geen weekpas verbruikt, voor niemand. Dezelfde bug is voor `groups.tz` al gedicht in 0019 §5; de profieltabel is toen overgeslagen. ✅ **De helft is gerepareerd:** de lus vangt de fout nu op, slaat het profiel over en telt het als `overgeslagen` in het antwoord. Geverifieerd met een profiel op `Mars/Olympus_Mons`: `{"gemist":1,"gered":1,"overgeslagen":1,"profielen":3}` — de rest van de job draaide gewoon door. **Wat nog ontbreekt is de CHECK op `profiles.tz` zelf**, en dat is een wijziging aan een bestaande tabel. Staat als **A38** in `docs/Q-TODO.docx`. | ~~Hoog~~ half gerepareerd |
| 2026-08-19 | ✅ Het scherm "Vandaag" haalde onophoudelijk gegevens op | Gevonden door de code-review op QS8-75, en het bestond al vóór deze issue. De dependency-lijst van het `useEffect` bevatte `afTeSluiten` en `cyclus` — objecten die `huidigeCyclus()` en `afsluitbareCyclus()` elke render vers opbouwen. Ophalen → `setWeekdoelen` → render → nieuwe objectidentiteit → ophalen, zonder eind. Onzichtbaar in de app (de gegevens zijn elke ronde gelijk en `loading` staat al uit), zichtbaar op de rekening: dit is een gratis Supabase-tier. Het venijnige is dat de comment erboven het probleem al beschreef en de lijst eronder precies het tegenovergestelde deed — `afTeSluitenStart` en `cyclusStart` bestaan alleen maar om dit te voorkomen. Gedicht; de stringversies staan er nu alleen. **Les: een comment die uitlegt waarom iets zo moet, bewijst niet dat het zo is.** | ~~Hoog~~ opgelost |
| 2026-08-19 | ✅ `verdien_weekpassen()` gebruikte een datum als identiteit van een aantal | Gevonden door beide technische reviews op QS8-81. 0039 bepaalde het *recht* op een pas uit een áántal (`count(distinct cycle_start_date)`) maar de *identiteit* van de uitgifte uit een dátum (`max(cycle_start_date)`). Die twee lopen uit elkaar zodra weken niet op volgorde worden goedgekeurd, en dat gebeurt gewoon. Twee gevolgen, allebei bevestigd met een proef: (1) wie week 12 als eerste laat goedkeuren heeft bij `voltooid = 6` en `voltooid = 12` dezelfde max-datum, waarna de unieke index de tweede rij via `on conflict do nothing` slikte — **een verdiende pas verdween stil**; (2) `trek_goedkeuring_in()` kan `voltooid` omláág zetten, dus intrekken en een ándere week goedkeuren leverde een tweede cadeau op — **één voltooide cyclus, twee passen**. Gerepareerd in 0042 door het model om te draaien: recht = `1 + voltooid / 6`, er wordt nooit meer uitgegeven dan het recht, en de sleutel is de n-de goedgekeurde cyclus op datum in plaats van de laatste. Idempotent op de inhoud in plaats van op een sleutel. ⚠️ **Productwijziging die daarbij hoort:** een pas die je verdient terwijl je voorraad vol is, vervalt niet meer maar komt vrij zodra je er een verbruikt. "Vervallen" kan niet zonder een kolom die bijhoudt dát er iets vervallen is — en zonder die kolom is vervallen niet te onderscheiden van fout (1). Staat als **A32** in `docs/Q-TODO.docx`. | ~~Hoog~~ opgelost |
| 2026-08-19 | ✅ De weekpasmelding kon nooit afgaan | Gevonden door de gebruikersreview op QS8-81, en het is de vervelendste soort: alles was groen. `app/(tabs)/index.tsx` gaf de melding de **afsluitbare** cyclus mee, terwijl `laatst_verbruikt` de week is die de rollover heeft dichtgezet — en die raakt uitsluitend weken met `cycle_start_date < afsluitbaar.startDate`. Die twee datums zijn dus per definitie nooit gelijk en `weekpasReddeDezeCyclus()` gaf altijd `false`. Gevolg: de gebruiker zag wél zijn minpunt en niet de geruststelling, op precies het moment waarop iemand de app weggooit. De unittests waren groen omdat ze de functie twee met de hand gelijkgemaakte datums voerden — **ze bewezen dat de vergelijking werkt, niet dat de aanroeper het juiste meegeeft.** Gerepareerd met `zojuistAfgeslotenCyclus()` in `modules/goals/cycles.ts`, dat het verschil in zijn documentatie uitlegt. Daar bovenop een tweede voorwaarde: de melding komt alleen als de reeks daadwerkelijk nog loopt, want bij twee gemiste weken en één pas stond "een weekpas heeft je reeks gered" onder een teller die "Nog geen reeks" zei. | ~~Hoog~~ opgelost |
| 2026-08-19 | ✅ `supabase/functions/` staat buiten typecheck, lint én CI | Gevonden door de code-review op QS8-81. `eslint.config.js` negeert `supabase/*` en `tsconfig.json` sluit `supabase/functions` uit, dus de rollover-code is door geen enkele automatische controle gegaan — terwijl daar wel de puntenboeking en sinds QS8-81 ook het weekpasverbruik in staat. Er is bovendien geen workflow die de functie deployt: wie een migratie toepast maar de deploy vergeet, heeft een half werkende feature zonder één signaal. Dat is valkuil 15 met een tweede gezicht. Voorstel voor de engineer-review: een aparte `deno check` in CI op `supabase/functions`, en de deploy aan de rollover-workflow hangen.  📏 **Nagemeten op 25-08-2026 en staat nog.** `tsconfig.json` sluit `supabase/functions` nog uit en `eslint.config.js` negeert `supabase/*`. ⚠️ Deze rij heeft zich sindsdien één keer bewezen: de `excused`-bevinding van 20-08 werd gemist doordat de schrijver een Edge Function was en de zoektocht in `pg_proc` plaatsvond.  ✅ **Gesloten op 25-08-2026.** `supabase/functions/deno.json` maakt de map één Deno-project, en CI heeft een eigen `edge`-job met `deno check` en `deno lint` — plus een tak in de poort, want zonder die tak wacht de poort wel op de job maar kijkt hij niet naar de uitslag, en dan is CI weer een suggestie. De enige lintregel die uit staat is `no-import-prefix`, met reden: Supabase schrijft de inline `jsr:`-specifier juist voor. ⚠️ **Lokaal was `deno check` niet volledig te draaien** — de agent-proxy geeft 403 op `jsr.io` — dus de zeven modules zónder externe import zijn hier gecontroleerd (alle zeven schoon) en de rest is voor het eerst in CI bewezen. ⚠️ **En het zoeken leverde een groter risico op dan de bevinding zelf:** `supabase/functions/_shared/time/` is een **tweede exemplaar** van `src/shared/time`, want Deno kan niet uit `src/` importeren. De 24 gedeelde functies zijn vandaag identiek, maar niets hield ze zo — repareer je een DST-fout aan één kant, dan rekent de nachtjob stilletjes andere weekgrenzen dan de app, en correctheidsregel 7 noemt precies dat als de fout die je een gebruiker kost. `npm run edge:controle` bewaakt het nu, geijkt in `tests/scripts/edge-tijd.test.ts` en met de hand rood gemaakt door één teken in `addDays()` te veranderen. Weglaten mag (de Edge-kopie is bewust een subset), afwijken niet. | ~~Middel~~ opgelost |
| 2026-08-19 | De rollover schaalt niet en logt gebruikers-id's | `.from('profiles').select(...)` zonder paginering en zonder `order`: boven de standaard rijlimiet van PostgREST slaat de job stilzwijgend een deel van de gebruikers over. Daarbovenop één query per profiel plus een `breathers`-query én een RPC per openstaand weekdoel. Bij het schaaldoel van 100k gebruikers is dit uurlijkse werk de duurste plek van het product. Los daarvan staan er gebruikers-id's in de foutlogboeken — geen domeinregel-7-lek (logs zijn niet groepszichtbaar), wel persoonsgegevens in een systeem met een andere bewaartermijn dan de database. Allebei `// TODO(paid-tier)`-materiaal. **Wordt zwaarder als:** het aantal profielen de standaard rijlimiet van PostgREST nadert (1000). Daarboven slaat de job stilzwijgend gebruikers over — geen fout, geen melding, gewoon geen rollover voor de rest. | Laag |
| 2026-08-19 | De weekpasstand woont als type in `shared/ui` | Opgemerkt door de code-review: `modules/goals/stand.ts` en `weekpas.ts` importeren `WeekpasStand` uit `shared/ui`, dus de datalaag wijst naar de presentatielaag. Het is een `import type` en dus vandaag geen runtime-koppeling. **Bewust niet veranderd**, want het is exact het patroon dat `KettingStand` sinds QS8-80 volgt (`modules/buddies/ketting.ts` importeert dat type ook uit `shared/ui`). Eén van de twee is verkeerd en het zijn er twee; dat maakt het een conventievraag voor de engineer-review, geen bug in deze issue. **Wordt zwaarder als:** het een runtime-import wordt in plaats van een `import type`. Nu kost de koppeling niets; dan wijst de datalaag echt naar de presentatielaag. | Laag |
| 2026-08-19 | ✅ A39 — `markeer_doorgeschoven()` repareerde een gemiste reeks gratis | **Gevonden door de security-review op 0043, en het is de reden dat A36 niet afgevinkt staat.** `markeer_doorgeschoven()` (0023) accepteert `status in ('todo','missed')` en zet `carried`; hij is `grant execute … to authenticated`. `herbereken_reeks()` (0014) breekt de reeks **alleen** op `missed` — `carried` valt in geen enkele tak en is dus neutraal. Eén RPC-aanroep per gemiste week herstelt de reeks daarmee met terugwerkende kracht, zonder weekpas, onbeperkt, en de groep ziet die herstelde reeks via `group_visible_streaks`. Erger nog: de rollover heeft de weekpas op dat moment al verbruikt, dus doorschuiven is **goedkoper dan de munt die QS8-81 introduceert**. Vandaag alleen via de API bereikbaar — `schuifDoor()` staat in `modules/goals/weekly.ts` maar geen enkel scherm roept hem aan (QS8-47 staat nog open). **Besluit nodig, want dit is productgedrag en geen bug:** mag doorschuiven alleen vóórdat de week sluit (`status = 'todo'`), of moet `carried` de reeks óók breken tenzij er een pas op die cyclus staat? ✅ **GEDICHT in 0045 (19-08), na besluit van Quinten.** Doorschuiven blijft mogelijk — QS8-47 vraagt er letterlijk om — maar het repareert je reeks niet: `carried` gaat in `herbereken_reeks()` dezelfde tak in als `missed`, inclusief de weekpas-ontsnapping. Wie zijn reeks wil redden zet er een pas op. ⚠️ En `markeer_doorgeschoven()` accepteert voortaan **alleen `missed`**, om twee redenen: een `todo` in de lópende cyclus doorschuiven maakt een duplicaat in dezelfde week én zou de reeks nú breken voor een week die nog loopt, en een `todo` in een verstréken cyclus doorschuiven vóór de rollover ontliep het minpunt (die job raakt alleen `todo` en `cancelled`, dus een rij die al `carried` is wordt nooit meer `missed`). De rollover krijgt nu altijd eerst zijn beurt; dat kost hooguit een uur en `schuifDoor()` zegt dat met zoveel woorden. | ~~Kritiek~~ opgelost |
| 2026-08-19 | ✅ A40 — de `todo`-rij verwijderen vóór de rollover deed hetzelfde | Zelfde ronde. De rollover raakt uitsluitend `status = 'todo'`, en precies die status is sinds 0043 verwijderbaar — terecht, want een verkeerd aangemaakt weekdoel moet je kunnen weggooien. Maar wie zondagavond weet dat hij zijn week niet haalt en zijn weekdoel wist, heeft die week nooit gemist: geen `missed`-rij, geen minpunt, geen onderbreking in de reeks. Het venster is de hele cyclus plus de coulanceperiode. **Dit was vóór 0043 net zo goed mogelijk**, dus het is geen regressie — maar het betekent wel dat A36 het effect niet afdekt dat A36 beschrijft. De onderliggende vraag is een productvraag: **is een verwijderd weekdoel hetzelfde als een weekdoel dat er nooit was?** Zeg je nee, dan moet afwezigheid in een verstreken cyclus meetellen, of moet verwijderen begrensd worden tot de lopende cyclus, of moet er een tombstone komen. ✅ **GEDICHT in 0045 (19-08), na besluit van Quinten: verwijderen wordt afsluiten.** De rij verdwijnt niet meer maar krijgt status `cancelled`, en het DELETE-recht is ingetrokken (dus een rechtstreekse DELETE geeft luid 42501 in plaats van stil niets te doen). ⚠️ De elegantie zit erin dat `cancelled` zich gedraagt als `todo` en niet als een nieuw geval: de rollover veegt hem bij het verstrijken van de cyclus mee naar `missed`, mét minpunt, en een weekpas kan hem redden als elke andere gemiste week. Daardoor hoeft `herbereken_reeks()` niets van `cancelled` te weten, blijft een afgesloten weekdoel in de lópende cyclus neutraal, en is er nergens een berekening nodig van "is deze cyclus al voorbij" — wat in SQL ook niet mag (correctheidsregel 7). Sluit aan op QS8-47, dat naast doorschuiven al letterlijk "of afsluiten" noemt. ⚠️ `cancelled` is óók uit `weekly_goals_select` gehaald voor groepsgenoten: een opgegeven week is exact het tegenslagsignaal dat 0019 en 0020 voor `missed` en `carried` weghaalden. | ~~Kritiek~~ opgelost |
| 2026-08-19 | ✅ `points_miss = 0` maakte missen gratis | Gevonden door de security-review op 0043, en het was een gat dat die migratie zelf opende door de punten-kolommen bewust insertable te laten met als argument "de CHECK uit 0007 begrenst ze". Voor `points_ceiling` en `points_floor` klopt dat; voor `points_miss` niet, want die CHECK is `between -5 and 0` en **nul zit erin**. Eén insert met `{"points_miss": 0}` en een gemiste week kost nooit meer een punt — de rollover boekt letterlijk `delta: weekdoel.points_miss`. Dat is domeinregel 10 ("week gemist −1") en het ondergraaft precies wat de weekpas moet betekenen. Gedicht in 0044 door de kolom uit de insert-grant te halen; de standaardwaarde −1 doet het werk, met een test erop. ⚠️ De CHECK zelf staat nog op `between -5 and 0` en bewaakt ook `service_role`; hem aanscherpen naar `between -5 and -1` is een wijziging aan een bestaande tabel en staat als voorstel bij A39. **De les: "er staat een CHECK op" is geen onderbouwing zolang je de grenzen van die CHECK niet hebt gelezen.** | ~~Kritiek~~ opgelost |
| 2026-08-19 | ✅ 0043 was niet idempotent, en de valkuil-17-controle klopte niet | Twee nakomers uit dezelfde review. (1) 0043 had wél een `drop policy if exists` vóór het weghalen van `weekly_goals_write` maar niet vóór de drie nieuwe policies, dus een tweede run faalde — onwrikbare regel 20, en een reële voetangel omdat valkuil 15 zegt dat de standaardreactie op repo-drift "draai hem opnieuw" is. (2) De kop van 0043 beweerde dat in `tests/` alleen `weekpassen.test.ts` als cliënt naar `weekly_goals` schrijft; er zijn er acht meer, in `policies.test.ts`, `besluiten.test.ts`, `epic7.test.ts` en `epic8.test.ts`. Geen daarvan stuurt `status` mee, dus er brak niets en de suite bleef groen — **de conclusie klopte bij toeval en de controle niet.** Dezelfde vorm als valkuil 18: de volgende lezer gelooft de zin in plaats van hem te herhalen. Allebei rechtgezet (0044 en de kop van 0043). | ~~Middel~~ opgelost |
| 2026-08-19 | ✅ Geen bovengrens op het aantal weekdoelen per gebruiker | Opgemerkt tijdens de review op 0043. `cycle_start_date` en `cycle_index` zijn vrij te kiezen, er is geen unieke constraint op `(goal_id, cycle_start_date)` — alleen een niet-unieke index — en geen rate limit. Eén ingelogd account kan in een lus tienduizenden rijen invoegen op een tier van 500 MB. Punten- en weekpasinflatie zit er níét in (beide tellen `distinct cycle_start_date`), dus dit is opslagmisbruik en geen scoremisbruik. Hetzelfde argument dat 0023 §3 zelf maakt over `completions`. Hangt samen met A37: een unieke constraint op `(goal_id, cycle_start_date)` zou dit én de dubbelzinnigheid in `herbereken_reeks()` in één keer oplossen — maar dat is een datamodelwijziging.  ✅ **Gesloten op 25-08-2026, migratie 0083.** `weekly_goals_insert` weigert nu boven tweehonderd weekdoelen per gebruiker per etmaal — beveiligingsregel 5, dezelfde vorm als `join_group_with_code` (0008). ⚠️ **De unieke constraint die deze rij voorstelde, kan niet meer:** besluit A37 (24-08) staat twee weekdoelen op hetzelfde doel in één week juist toe en 0074 rekent daarmee. En een grens per cyclus zou de lus niet stoppen, want `cycle_start_date` is vrij te kiezen — die telt de datum op en gaat door. Alleen een grens op wat iemand per dag aanmaakt bindt hem. ⚠️ In de policy en niet in een trigger: `weekly_goals_insert` geldt alleen voor `authenticated`, dus de rollover komt er als `service_role` langs zonder dat daar een uitzondering voor nodig is — een trigger zou voor beide gelden, en een trigger die op een rolnaam beslist faalt open. `weekdoelen_vandaag()` faalt dicht bij een lege `auth.uid()`. Twee tests, allebei met de hand rood gemaakt, en de tweede bewijst juist dát de systeemclient er langs mag. | ~~Middel~~ opgelost |
| 2026-08-19 | ✅ Een verschuldigd commitment verdwijnt met het doel | Bijvangst van de review op 0043. `commitments.goal_id … on delete cascade` (0001) betekent dat één DELETE op een doel ook een commitment wist dat al `due` is — en domeinregel 11 zegt juist dat de begunstigde groep op dat moment leesrecht krijgt. Verwant: `goals_delete` omzeilt de nieuwe `weekly_goals_delete`-policy, want een referentiële actie slaat de RLS van de kindtabel over. Dat laatste levert geen winst op (reeks, weekpassen en breathers gaan mee), maar de belofte "geschiedenis blijft staan" geldt daarmee op rijniveau en niet op ouderniveau. Hoort bij EPIC 9.  ✅ **Gedicht door migratie 0058 (21-08); deze rij stond alleen nog niet bijgewerkt** — zie de rij van 21-08 hieronder voor wat er precies gebeurd is. ⚠️ De nieuwe bevinding die 0058 er zélf onder schreef — *drie kopieën van één lijst* — is op 25-08 gesloten met migratie 0084: `commitment_zichtbaar_voor_groep()` is nu de enige bron, en zowel `commitments_select` als `verwijder_doel()` roept hem aan in plaats van de lijst over te schrijven. De derde kopie, `isAfgegaan()`, staat in de client en kan geen SQL aanroepen; die blijft een kopie maar wordt sinds 0084 tegen de bron getoetst in plaats van tegen zichzelf — het onderscheid dat 0032/0034 groen liet blijven. ⚠️ Het lichaam van `verwijder_doel()` is overgenomen uit `pg_get_functiondef()` en niet uit 0058: de gedeployde functie had er sindsdien een `bedenktijd()`-toets bij, en uit het bestand overschrijven had die stil teruggedraaid. | ~~Middel~~ opgelost |
| 2026-08-21 | ✅ `commitments_update` had geen `with check`, en dus kon niemand ooit een commitment intrekken | Gevonden bij het bouwen van EPIC 9, en aangetoond tegen de echte database in een teruggedraaide transactie. Postgres gebruikt de `using`-expressie óók als controle op de nieuwe rij zodra er geen `with check` staat. `commitments_update` had `using (status = 'set' and eigenaar)`, dus `status = 'cancelled'` schrijven schond de check en gaf `42501` — precies de enige statusovergang die de client wél hoort te kunnen maken. `trekIn()` in `src/modules/commitments/api.ts` heeft dus nooit gewerkt; de knop stond er sinds QS8-35 en gaf een storingsmelding. Migratie 0006 wilde voorkomen dat de client zelf een status kiest en nam die ene overgang mee. Gedicht in 0057 met een expliciete `with check (status in ('set','cancelled'))`, plus een kolomgrant zodat `type`, `confirmed_at` en `beneficiary_group_id` niet meer te schrijven zijn — RLS kan geen kolommen beperken, en zonder die grant is een straf te ontlopen door hem tot beloning te verklaren. **De regel die eruit volgt: een `using` zonder `with check` op een UPDATE-policy is bijna nooit wat je bedoelt. Schrijf hem altijd uit, ook als hij hetzelfde zou zijn — dan is het zichtbaar een keuze in plaats van een weglating.** | ~~Hoog~~ opgelost |
| 2026-08-21 | ✅ `commitment_events` weigerde elke insert, dus het auditspoor bestond niet | Zelfde ronde. De tabel heeft RLS aan met **alleen** een SELECT-policy, en `logCommitmentEvent()` schreef ernaartoe vanuit de client en slikte de `42501` op via `reportError`. De tabel stond op nul rijen, terwijl QS8-84 acceptatiecriterium 7 een volledig auditspoor eist en domeinregel 5 zegt dat een commitment auditeerbaar moet zijn. Gedicht in 0057, maar niet met een INSERT-policy: het schrijven is naar een trigger verhuisd (`commitments_audit` → `noteer_commitment()`), met `auth.uid()` als actor. Daarmee is het auditspoor een schema-eigenschap zoals `confirmed_at NOT NULL` dat al was — niet over te slaan en niet te vervalsen, en `actor_id IS NULL` betekent nog steeds systeem. **De klasse is dezelfde als bij `weekly_goals`: een client die zijn eigen bewijsvoering bijhoudt, houdt hem bij tot het hem uitkomt.** | ~~Hoog~~ opgelost |
| 2026-08-21 | ✅ Een verschuldigd commitment verdwijnt met het doel — nu gedicht | De bevinding van 19-08 hierboven ("hoort bij EPIC 9") is opgepakt in migratie 0058. `verwijder_doel()` weigert nu zodra er een commitment in `unlocked`, `due` of `resolved` staat — exact de lijst uit `commitments_select`, dus exact de standen waarop de begunstigde groep al meeleest. `set` en `cancelled` blijven verwijderbaar: die zijn nooit buiten het eigen scherm geweest. De cascade op de foreign key blijft staan en is goed; het onderscheid is een voorwaarde en geen referentiële actie. ⚠️ Loopt die lijst uit de pas met `commitments_select`, dan wist je hier weer iets wat de groep gezien heeft — er staat een test op in `tests/rls/epic9.test.ts`, en `isAfgegaan()` in `stand.ts` is dezelfde lijst voor de UI. **Drie kopieën van één lijst; als er een vierde bij komt hoort er een gedeelde bron te zijn.** | ~~Middel~~ opgelost |
| 2026-08-21 | ✅ Een UPDATE die op de `using` afketst, geeft géén fout — en een test die op `42501` let, ziet dat niet | Uit het testen van 0057, en het kostte bijna een vals groen vinkje. Alleen een schending van de `with check` geeft `42501`; ketst de rij af op de `using`, dan filtert RLS hem weg en raakt de UPDATE nul rijen met HTTP 204. De eerste versie van de proef op 0057 concludeerde daaruit "gelukt (fout!)" waar de policy juist correct weigerde. Dit is dezelfde vorm als de bevinding van 19-08 over `week_pass_events` ("een ontbrekende policy weigert stil"), maar nu binnen één policy: **dezelfde policy weigert luid of stil, afhankelijk van welke helft afketst.** `tests/rls/epic9.test.ts` toetst daarom per geval bewust het één of het ander, met een comment erbij welke. Voor de engineer-review: is dit als conventie vast te leggen, of hoort er een helper te komen die "deze update mag niet" uitdrukt zonder dat de schrijver moet weten welke helft afgaat?  ✅ **Beantwoord op 25-08-2026, en het is de helper geworden en niet de conventie.** `magNietLanden(schrijf, lees)` in `tests/rls/harness.ts` drukt "deze schrijfpoging mag niet landen" uit zónder dat de schrijver hoeft te weten welke helft van de policy afgaat: hij leest de rij vóór en ná via `adminDb()` en toetst dat er niets veranderd is. Dat is de enige eigenschap die in béide gevallen klopt. ⚠️ Drie dingen die de helper hard maakt in plaats van beleefd: een foutcode buiten `42501`/`23514`/`P0001` is een rode test (een typo in een kolomnaam geeft óók een fout en verandert óók niets), een `lees()` die niets teruggeeft is een rode test ("onveranderd" is gratis als het filter niets raakt — dan bewijst de test een lege selectie en geen slot), en een wijziging ondanks een foutcode is er ook een. `tests/rls/harness.test.ts` ijkt alle vijf de gevallen met de hand, zonder database. De twee `if (error !== null)`-plekken in `weekpassen.test.ts` zijn omgezet. | ~~Middel~~ opgelost |
| 2026-08-21 | ✅ `goals.status = 'missed'` wordt door niets gezet, maar is wél groepszichtbaar | Opgemerkt bij EPIC 9. `goals_status_valid` staat `missed` toe en `goals_select` laat groepsgenoten de statuskolom van een gekoppeld doel lezen — de comment in `zetArchief()` benoemt dat zelf al als reden waarom `authenticated` sinds 0035 niet meer op die kolom mag schrijven. Vandaag zet niets die waarde, dus het is theorie. Maar het is precies de vorm van de zwaarste bevinding uit EPIC 5 (`weekly_goals.status`), en de rollover is de plek waar iemand hem logischerwijs zou gaan zetten zodra "een doel waarvan de deadline verstreek" een eigen weergave krijgt. **Besluit vóór iemand hem vult, niet erna** — dezelfde vorm als de aantekening bij A17, die bewezen heeft dat zo'n "herbevestigen vóór X" werkt. Twee richtingen: `missed` uit de statuslijst halen zolang niemand hem gebruikt, of de statuskolom uit `goals_select` houden zoals 0050 met `risk_status` deed. ✅ **Gesloten op 25-08-2026, migratie 0082 — en dit is het tweede bewijs dat "besluit vóór iemand hem vult" werkt.** `missed` is uit `goals_status_valid` gehaald. Van de twee richtingen die hierboven staan is dit de goedkope: de statuskolom afschermen zou het groepsoverzicht en De Ketting raken, en dat voor één waarde die niemand zet. ⚠️ Sinds A41 is de vraag bovendien zwaarder geworden: zou `missed` ooit gezet worden, dan hoort hij te variëren op `groups.zichtbaarheid` zoals 0077 t/m 0079 dat voor de weekdoelen, de reeks en De Ketting deden — een ontwerp, geen kolomgrant. Door de waarde weg te halen kan die beslissing niet meer overgeslagen worden: terugzetten is een migratie, en een migratie vraagt een lezer. ⚠️ Eén plek zette hem wél, en dat was geen productiecode maar een fixture in `tests/rls/epic7.test.ts`, die bewees dat een gemist doel geen systeembericht gaf. Die helft is nu `archived` — sterker dan wat er stond, want dat is de enige statusovergang naast `completed` die een gebruiker echt kan bereiken. `check_waarden()` (0082) maakt de CHECK leesbaar vanuit een test, zodat `STATUSSEN` in de app en de database in **beide** richtingen vergeleken worden — de les van 0032/0034, waar de test de app-lijst met zichzelf vergeleek. | ~~Middel~~ opgelost |
| 2026-08-21 | Drie backend-issues op rij bleken geen enkele aanroeper te hebben | QS8-47, QS8-112 en nu EPIC 9. Bij dit laatste lag het twee lagen diep: `meld_commitment()` (0025) wachtte op een status die niets ooit zette, en dáárónder kon `goals.status` helemaal geen `completed` worden, dus `meld_doel_af()` had evenmin ooit gedraaid. Twee triggers die maandenlang in de database stonden zonder één keer af te gaan, allebei met tests eromheen die het losse gedrag wél bewezen. **De vorm is steeds dezelfde: het onderdeel is getest, de keten niet.** Suggestie voor de review: een controle die elke SECURITY DEFINER-functie en elke trigger opsomt die door geen enkel pad in `src/` of `app/` bereikbaar is — dat is statisch af te leiden en zou alle drie deze gevallen gevonden hebben.  🗣 **Beoordeeld op 25-08-2026: niet te meten, en juist daarom belangrijk.** Dit is een procesbevinding over dóde ketens — code waar niets kapot aan is omdat er niets langskomt. Geen enkele query vindt dat. ⚠️ **Sinds deze rij is het nóg twee keer voorgekomen**, allebei vandaag gevonden: `goals.status = 'missed'` (0082) en `scope_reduced`/`milestone_dropped` in `goal_events` (0087) — CHECK-waarden die niemand ooit schreef, allebei groepszichtbaar. De vraag uit onwrikbare regel 18 blijft het enige gereedschap: *kan een gebruiker hier daadwerkelijk bij, en langs welke knop?* 📏 **De suggestie hierboven is op 25-08-2026 gebouwd, en de rij blijft open:** `npm run keten:controle` draait mee in CI en in `/audit`. Twee controles, want de vijf gevallen zijn twee soorten — een functie die niemand aanroept (QS8-47, QS8-112) en een CHECK-waarde die niemand schrijft (0082, 0087). ⚠️ **Tests en scripts tellen níét als aanroeper, en dat is de hele truc:** bij EPIC 9 stonden er tests omheen die het losse gedrag bewezen terwijl geen enkele knop erheen liep. ⚠️ **Wat hij niet vindt, staat in de kop van het script.** EPIC 9 zelf hád een aanroeper — een trigger die netjes aan een tabel hing en wachtte op een status die niets ooit zette. Dat is een dode keten op wáárde-niveau bínnen een functie en statisch niet af te leiden; de controle dekt daarvan alleen de helft die in een CHECK staat. Van de vijf gevallen zou hij er dus vier gevonden hebben, niet vijf. ⚠️ Het bouwen kostte twee valse-alarmrondes die het opschrijven waard zijn: acht functies die via `execute function public.…()` aan een trigger hangen, en een constraint die sinds 0050 niet meer bestaat omdat de kólom is gedropt. Allebei staan ze nu als ijkingsgeval in `tests/scripts/dode-keten.test.ts`, en de controle is met de hand rood gemaakt tegen de echte migraties. **Deze rij blijft Hoog**: het gereedschap vangt de makkelijke helft, en de vraag uit regel 18 blijft het werk. | **Hoog** |
| 2026-08-21 | ✅ De Nederlandse zin van een systeembericht zat vast in een onveranderlijke rij | QS8-107 stap 2, migratie 0059. `chat_messages` bewaarde naast `system_event` ook een uitgeschreven Nederlandse zin in `body`, en dát is wat het scherm toonde. Een chatbericht is een onveranderlijke kopie (beslisdocument 002 §3), dus die zin was er na de eerste echte gesprekken niet meer uit te krijgen: wie ooit een tweede taal aanzet, krijgt een vertaalde app met Nederlandse regels in de geschiedenis. **Dit is voor de vierde keer dezelfde valkuil — een redenering die klopt zolang een tabel leeg is** (`chain_links` 0037, `week_pass_events` 0039, `commitments` 0057, nu deze). Verschil: deze keer op tijd, met 52 testrijen en nul mensberichten in de tabel. De parameters staan nu als kolommen in de rij en de zin wordt gemaakt in `src/modules/buddies/systeemberichten.ts`. | ~~Hoog~~ opgelost |
| 2026-08-21 | ✅ Een tweede persoon in een jsonb-veld overleeft het verwijderen van een account | Gevonden bij het uitwerken van 0059. Het besluit was "`subject_id` plus `payload`", maar `completion_approved` noemt twee mensen en de tweede paste dus in `payload` — tot bleek dat een uuid in jsonb geen foreign key heeft. `on delete set null` raakt hem niet, dus de naam van een verwijderd account blijft afleidbaar uit een rij die volgens 0031/0033 juist geanonimiseerd hoort te zijn. Opgelost met `actor_id` als volwaardige kolom. **De algemene regel: een verwijzing naar een persoon hoort nooit in een jsonb-veld, in geen enkele tabel.** Nog niet nagelopen of dat elders al gebeurt — `commitment_events.payload` en `goal_risk.reason` zijn de kandidaten. `commitment_events.payload` bevat vandaag alleen een `group_id`, geen persoon; `ai_jobs.input` is niet nagekeken.  ✅ **Nagelopen en gesloten op 25-08-2026, migratie 0085.** Alle tien jsonb-kolommen van het schema langsgelopen. Eén raak, en niet een van de twee genoemde kandidaten: `beslis_deadline_verzoek()` schreef `'approved_by' => auth.uid()` in `goal_events.new_value`, omdat `actor_id` al door de aanvrager bezet was — exact dezelfde beweging als bij `completion_approved` in 0059. ⚠️ **Erger dan een gemiste anonimisering, want die wérkt hier al één tabel verderop:** `deadline_requests.decided_by` heeft `on delete set null`, dus die kolom wordt netjes geleegd terwijl de kopie in jsonb blijft staan — en `goal_events_select` geeft elke groepsgenoot leesrecht. Bovendien was `approved_by` te verzinnen, want `goal_events_insert` laat een gebruiker een rij op zijn eigen doel schrijven. Nu een kolom met een foreign key, voor geen enkele client schrijfbaar. Schoon bevonden: `chat_messages.payload` (0059 gaf het `subject_id`/`actor_id`), `commitment_events.payload`, `goal_risk.reason`, `group_events`, `goal_interviews.answers`, `ai_jobs.input`/`output`. ⚠️ **En de regel is sinds vandaag een controle en geen zin:** `npm run persoon:controle` draait mee in `/audit` en toetst op de wáárde, niet op de sleutelnaam — `group_id` en `request_id` zijn geen personen, en een controle die op elke `_id` afgaat leer je negeren. Geijkt in `tests/scripts/persoon-in-jsonb.test.ts`, dat er twee echte fouten in mijn eigen script uit haalde. | ~~Middel~~ opgelost |
| 2026-08-21 | ✅ Een onveranderlijkheidstrigger sloopt een `on delete set null` — en het is nú de derde keer gebeurd | **Gevonden en gedicht op dezelfde dag (0059 → 0060), en de vondst is leerzamer dan de bug.** WERKVOORRAAD §8 punt 8 beschrijft deze val sinds 0033, migratie 0059 citeert hem in zijn eigen kop, past hem correct toe op `actor_id` — en vergeet hem één regel lager voor `subject_id`. **De aantekening lezen, opschrijven en er dan naast grijpen: dat is precies de faalvorm die CLAUDE.md beschrijft als "fouten worden gekopieerd", maar dan omgekeerd — de goede gewoonte werd niet gekopieerd naar de tweede kolom in dezelfde functie.** ⚠️ Het gedrag was bovendien erger dan in 0031: daar draaide de trigger de anonimisering stil terug, hier wordt de kolom teruggezet naar een id dat Postgres opnieuw tegen de foreign key houdt, en dan faalt de hele DELETE met `chat_messages_subject_id_fkey`. `verwijder_mijn_account()` viel dus om zodra je in één systeembericht genoemd werd — en dat ben je na één keer meedoen aan een groep. Aangetoond tegen het echte project in een teruggedraaide transactie. ⚠️⚠️ **De RLS-suite zou dit nooit gevangen hebben**, en dat is het echte agendapunt: `removeTestUsers()` gooit eerst de groepen weg, waarna de systeemberichten mee cascaderen vóórdat het profiel aan de beurt is. De opruiming verbergt de bug. Er staat nu een test in `epic7.test.ts` die een profiel weggooit terwijl zijn systeembericht blijft staan. **Voorstel voor de review: een controle die elke BEFORE UPDATE-trigger opsomt die een kolom met `on delete set null` hard terugzet.** Dat is statisch af te leiden uit `pg_constraint` plus de functiebron, en het had alle drie de keren gewerkt.  ✅ **Het voorstel is gebouwd op 25-08-2026, migratie 0086.** `onveranderlijkheid_bewaking()` somt elke BEFORE UPDATE-trigger op die een kolom met `on delete set null` hard terugzet, en zegt per kolom of de grendel uit 0060 erop staat — statisch uit `pg_constraint` plus `pg_get_functiondef()`, precies zoals deze rij voorstelde. ⚠️ **En hij vond meteen een vierde geval: `groups.created_by` in `guard_group_update()`.** Dat is vandaag géén bug, en dat is nagemeten en niet aangenomen — in een teruggedraaide transactie op het echte project een profiel gemaakt, een groep laten aanmaken en het account verwijderd: de DELETE lukt. Drie dingen houden het tegen: `authenticated` heeft geen UPDATE-recht op `created_by`, de cascade draait niet als `authenticated` (en dan stapt de trigger er bij de eerste regel uit), en `verwijder_mijn_account()` is `security definer`. ⚠️ **Maar die tweede reden is het construct dat één rij hoger al als "faalt open" staat genoteerd** (`guard_group_update` en `current_user`). De veiligheid van vandaag hangt dus aan een regel waarvan de review zelf kan besluiten dat hij weg moet. Drie regels grendel halen die afhankelijkheid eruit, en dat is goedkoper dan de aantekening die het drie keer niet gered heeft. ⚠️ **De gedragstest ziet dit niet en de structurele wél**, met de hand nagemeten: haal de grendel eruit en `tests/rls/policies.test.ts` wordt rood op de structuur terwijl het verwijderen van een account gewoon blijft werken. Dat is de hele reden dat de controle er is. | ~~Hoog~~ opgelost |
| 2026-08-22 | 🔴 A47: de RLS-suite is in een volle run onbruikbaar geworden | **Dit is nu het zwaarste punt op deze lijst.** De suite maakt ongeveer veertig aanmeldingen en Supabase weigert na ongeveer dertig. Op 22-08 gaven twee volle runs achter elkaar 1 respectievelijk 5 falende bestanden — **elke keer andere** — en élk betrokken bestand bleek in isolatie groen (epic3, epic7, epic8, epic9, policies, weekpassen, besluiten stuk voor stuk nagelopen). ⚠️ Wat je ziet is géén melding over rate limiting maar een fixture die halverwege omvalt, en dat leest als een kapotte policy. **Het echte risico is het faalbeeld en niet de vertraging:** een suite die soms zonder aanwijsbare reden rood is, leert je rood te negeren — en dan is de dag dat er écht iets stuk is, de dag dat je het mist. Zolang dit staat, is "npm test is groen" geen bewijs meer over domeinregel 7. Twee richtingen: één set fixtures voor de hele run in plaats van per bestand, of een eigen testproject met een eigen quotum. Het eerste is goedkoper en raakt `tests/rls/harness.ts`; het tweede is schoner maar vraagt een tweede Supabase-project en dus een besluit over kosten.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Opgelost door de lokale opstelling (QS8-116 → QS8-119). `scripts/lokale-stack.sh` bouwt het schema op een eigen database en `shim_maak_gebruiker()` vervangt GoTrue, dus een volledige run doet **nul** aanmeldingen bij Supabase. Er is geen limiet meer om overheen te gaan. ⚠️ Dit stond hier als het zwaarste punt van de lijst en was al opgelost — precies de reden dat er nu een stempel op elke gemeten rij staat. | ~~Kritiek~~ opgelost |
| 2026-08-22 | ✅ Een module-constante met vertaalde tekst bevriest de taal op importtijd | Uit QS8-115. Een `const` die `t()` aanroept, wordt één keer opgebouwd bij het importeren van de module — en dat is vóórdat het profiel geladen is. Iemand met Engels ingesteld houdt dan Nederlandse teksten tot hij de app herstart, en er is niets aan te zien: de tekst klópt, hij is alleen van de verkeerde taal. Er waren er tien in dit project (`BEVESTIGING`, `VIERINGEN`, `PUNTEN_UITLEG`, `WEEKPAS_UITLEG`, `STATUS_TEKST`, `BEWIJSEIS_LABELS`, `HUDDLEDAGEN`, `VRAGEN`, `CATEGORIE_LABELS`, `INTERVIEW_STAPPEN`) plus zes `Readonly<Record<string, string>>`-meldingentabellen. Allemaal functies geworden. ⚠️ **Ik heb er zelf twee geïntroduceerd in dezelfde sessie waarin ik de regel opschreef** — `VERWIJDER_MELDING` en `VRAGEN`. Dat is het argument om dit als lint-regel af te dwingen in plaats van als conventie: een `t()`-aanroep op moduleniveau is statisch te vinden.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Er is geen enkele module-constante meer die `t()` op importtijd aanroept — gegrepen over `src/` en `app/`. QS8-115 heeft ze alle omgezet naar functies. | ~~Middel~~ opgelost |
| 2026-08-22 | ✅ Zod-schema's zijn een tweede, onzichtbaardere versie van hetzelfde | Zelfde ronde. Een schema wordt op moduleniveau opgebouwd, dus `{ error: t('...') }` legt de melding vast bij het importeren. Het schema werkt, de validatie klopt, alleen de taal is bevroren. Zod v4 accepteert een functie (`{ error: () => t('...') }`) en roept die bij élke parse opnieuw aan. Geverifieerd door één schema tijdelijk terug te zetten naar de gretige vorm; de test werd rood op precies die regel. Dezelfde lint-regel als hierboven zou dit ook vangen.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** Alle schema's gebruiken nu de thunk-vorm `{ error: () => t('…') }`, dus de melding wordt op validatiemoment opgebouwd en niet op importtijd. Gecontroleerd in `auth/schemas.ts`, `completion-schemas.ts` en `weekly-schemas.ts`. | ~~Middel~~ opgelost |
| 2026-08-22 | De taal van een melding hoort bij de ontvanger en niet bij het proces | Gevonden bij het overzetten van `notifications/regels.ts`. `shared/i18n` houdt één taal vast voor het hele proces — juist op een telefoon met één gebruiker, fout in een job die over álle profielen loopt: iedereen zou de taal krijgen van wie er toevallig als laatste is ingesteld. Die fout is onzichtbaar, want er kómt gewoon een melding aan. `regels.ts` neemt daarom een taalparameter en gebruikt `shared/i18n` bewust níét; de job leest `profiles.locale` per profiel. ⚠️ **Deze scheiding geldt voor élke server-side tekst die nog komt** — een cyclusoverzicht per e-mail, een export, een webhook.  ✅ **Nagemeten op 25-08-2026.** Opgelost aan beide kanten. `supabase/functions/notificaties/index.ts` leest `locale` per profiel mee en kiest de taal per ontvanger; en sinds QS8-113 bestaat het schrijfpad ook — `opslaanProfiel()` schrijft `locale`, met `TALEN` als bron en de CHECK `profiles_locale_bekend` ernaast. | ~~Middel~~ opgelost |
| 2026-08-22 | ✅ Twee domeinregel-7-beloftes stonden alleen in het Nederlands onder test | Uit QS8-115. De toelichting bij "Waar liep het eerder vast?" (*"je groep ziet dit nooit"*) en de hint bij vraag 2 van de weekafsluiting (*"om te helpen, niet om te beoordelen"*) zijn de twee velden waarin een gebruiker expliciet over tegenslag schrijft. Allebei staan ze nu in béíde talen onder test. **De algemene vorm is belangrijker dan deze twee gevallen: een vertaler kent de domeinregels niet.** Elke tekst die een belofte doet over wie iets te zien krijgt, hoort per taal getoetst te worden en niet alleen in het origineel.  ✅ **Nagemeten op 25-08-2026 tegen de gedeployde stand.** `src/shared/i18n/beloftes.test.ts` toetst de beloftes in **beide** talen, en is op 25-08 uitgebreid met de eis dat elke `_beschermd`-sleutel een `_open`-tegenhanger heeft die er ánders uitziet. | ~~Middel~~ opgelost |
| 2026-08-22 | Een niet-idempotent migratiescript dat twee keer draait | Bij QS8-115 liep een Python-hulpscript twee keer door een fout halverwege, waardoor het catalogusblok dubbel in `nl.ts` en `en.ts` kwam. TypeScript ving het (TS1117: dubbele property). Geen schade, wél dezelfde categorie als een SQL-migratie zonder rollback-pad — en onwrikbare regel 20 eist die idempotentie juist omdat de standaardreactie op een half gelukte run "draai hem opnieuw" is. Voor de review: geldt regel 20 ook voor eenmalige hulpscripts, of accepteren we daar wegwerpcode? **Wordt zwaarder als:** zo'n hulpscript ooit iets aanraakt wat de typecheck níét ziet — een JSON-bestand, een migratie, een `.env`, of data in de database. Nu is TypeScript het vangnet, en dat is toeval: het werkte omdat het toevallig TypeScript-bestanden waren. | Laag |
| 2026-08-24 | `chat_messages_insert` bewaakte `type` maar niet `system_event` | ✅ **Gedicht in 0071, dezelfde dag als de bevinding.** Was: de policy verbiedt sinds 0006/0010 `type = 'system'` (gat A5) maar zei niets over `system_event`. Een lid kon dus een eigen bericht plaatsen met een systeemgebeurtenis erop. Dat was ruim twee maanden onschadelijk, want zowel `ChatRegel.tsx` als `isSysteembericht()` kijken naar `sender_id is null` en `type` en niet naar `system_event` — er viel niets mee te vervalsen. **Migratie 0070 maakte het wél schadelijk**: `meld_ketting_mijlpaal()` telt de al gemelde mijlpalen, dus zeven zelfgeplaatste rijen zetten elke echte aankondiging van die groep permanent uit. Gedicht met twee sloten — de policy laat de handeling niet meer toe, en de telling accepteert alleen rijen die `plaats_systeembericht()` geschreven kan hebben. ⚠️ **De les is de vraag uit `CLAUDE.md` in zijn omgekeerde vorm.** Die zegt: kijk of er een weggelegde bevinding staat over de primitieve handeling waar je nieuwe beslissing op leunt. Hier stond er geen — het gat was nergens opgeschreven, juist omdát het geen gevolg had. **Een schrijfrecht zonder gevolg is geen veilig schrijfrecht; het is een gevolg dat nog niet gebouwd is.** | ~~Middel~~ opgelost |
| 2026-08-24 | De repository staat publiek | Gevonden op verzoek van Quinten om te controleren of het project afgeschermd is. `QS86-bot/GoalBuddies` staat op `public` sinds de aanmaak op 15-08-2026. **Er is geen credential gelekt** — de volledige historie van alle branches is afgezocht op JWT-, Supabase-, OpenAI-, AWS- en GitHub-tokens en op private keys, met nul treffers; `.env` is nooit gecommit, `.env.example` heeft lege velden, en de twee workflows met de service-role-key echoën hem nergens. 0 forks, 0 stars, 1 collaborator. **Wat wél open lag** is de broncode, alle documenten (dus de productstrategie) en — het zwaarst — `supabase/migrations/`: het complete schema met elke RLS-policy, inclusief de rollback-paden en koppen die vertellen welke gaten er ooit in zaten. ⚠️ **Bewust uitgesteld door Quinten (24-08) tot de software af is**, vastgelegd als **QS8-126**. Dat is een verdedigbare grens: er brandt niets vandaag. **Wordt zwaarder als:** er een tweede persoon toegang krijgt tot de repo, er een echte gebruiker aanmeldt, of er een sleutel in de historie belandt — vanaf dat moment is publiek niet langer alleen een IP-vraag. ⚠️ Wat al publiek was is niet terug te draaien: publieke repo-events worden permanent gearchiveerd door derden. **En één valstrik bij het omzetten:** privé kost Actions-minuten (2.000/maand gratis, minimaal één minuut per job), en de twee uurlijkse workflows alleen al vragen er ~1.460. Raken de minuten op, dan stopt de rollover — geen minpunten, geen `missed`, geen reeks die breekt.  📏 **Nagemeten op 25-08-2026 en staat nog.** Via de GitHub-API nagemeten: `"private": false`, `"visibility": "public"`. Ongewijzigd, en dit is een handeling die alleen Quinten kan doen. | **Middel** |
| 2026-08-24 | De stack lekte precies wat de melding net had weggehaald | ✅ **Gedicht op de dag van de vondst.** Was: `reportError()` haalde de foutmelding door `scrubMessage()` — e-mailadressen, tokens en de door Postgres geciteerde waarde eruit — en gaf `error.stack` daarnaast **ongewijzigd** door. De eerste regel van een stack ís die melding (`Error: Key (invite_code)=('zomer-2026') already exists`), dus alles ging er alsnog uit. Nagemeten in Node, niet vermoed. ⚠️ **Beide functies waren correct; de bedrading niet** — en dat is de reden dat er nu een test op de rand staat (`rapport.test.ts`) en niet alleen op de onderdelen. `scrub.test.ts` toetste `scrubMessage()` uitgebreid en bleef groen terwijl dit lek er de hele tijd was. Gerepareerd met `scrubStack()`, die de kop opnieuw ópbouwt uit de al geschoonde melding in plaats van hem nog een keer te schonen: twee schoonmaakpaden kunnen uit elkaar lopen, dit niet. ⚠️ Dit raakt QS8-24 criterium 3 én domeinregel 7: een crash bij het afsluiten van een gemiste week mocht niet met die tekst in een dashboard belanden. | ~~Hoog~~ opgelost |
| 2026-08-24 | De tijdzone werd op twee plekken zelf bepaald, en ze verschilden | ✅ **Gedicht, QS8-27 criterium 3.** Was: `voorgesteldeTijdzone()` in `modules/auth` deed `Intl.DateTimeFormat().resolvedOptions().timeZone` mét `try`/`catch` en een terugval; het aanmaken van een groep in `modules/buddies` deed dezelfde aanroep **zonder allebei** en gaf door wat `Intl` toevallig teruggaf. ⚠️ Dat tweede zet `groups.tz` — de groepsklok van domeinregel 1, die voor **iedereen in de groep** de huddledag, de weekafsluiting en De Ketting bepaalt. `resolvedOptions().timeZone` is in de typing een `string` maar geeft op oudere JavaScriptCore-versies `undefined`; de CHECK uit 0019 weigert dat, dus wat de gebruiker kreeg was een storingsmelding bij het aanmaken van zijn eerste groep. Nu één `apparaatTijdzone()` in `shared/time` met terugval en geldigheidstoets, en een derde plek (`tijdzoneSchema`) die dezelfde toets deelt. **Het slot is een lintregel** — `no-restricted-syntax` op `resolvedOptions()`, naast de bestaande regels voor `new Date()` en `Date.now()` — want een unittest op `apparaatTijdzone()` bewaakt niet dat er morgen geen tweede aanroeper bijkomt. Geproefd: de regel vuurt op precies de teruggezette regel. | ~~Middel~~ opgelost |
| 2026-08-24 | `npm run tekst:controle` staat groen terwijl er hardgecodeerde tekst staat | Gevonden tijdens EPIC 13, bij het bewerken van `app/groep/beheer/[id].tsx`. In dat ene bestand staan minstens vijf Nederlandse zinnen die de controle niet ziet: `label="Huddledag"` (één woord, en de propvariant eist er twee), een `hint={'...'}` die over meerdere regels loopt (de regex eist de sluitquote op dezelfde regel), `title:` en `body:` binnen het `empty={{ ... }}`-object (de regex kent alleen `title=`), `setMelding('Opgeslagen. Lopende kettingschakels...')` (geen prop en geen JSX-tekst), en `<Caption>Voorlezen kan ook: {toonCode(...)}</Caption>` (de accolade valt buiten het patroon). ⚠️ **Dit is precies de vorm van onwrikbare regel 18 vraag 3**: de controle bewaakt iets — proppatronen — maar niet de belofte "er staat nergens meer tekst hard in de code". De schrijver van `tekst-controle.mjs` heeft hem één keer naast een handmatige telling gelegd, en dat is te zien aan de vier ijkingen in het commentaar; deze vijf gaten zaten er daarna nog steeds in. ✅ **Gerepareerd op 24-08-2026, dezelfde dag.** Zeven vormen erbij: een prop met één woord dat met een hoofdletter begint, een tekstsleutel in een objectliteraal, een zin in `setMelding()`, JSX-tekst met een accolade erin, een prop die over meerdere regels loopt (via een toestand, want per regel is dat principieel niet te zien), plus twee valse-melding-filters (entiteiten, en waarden die geen taal zijn zoals `Europe/Amsterdam`). De oude ontsnapping `if (/\bt\(/.test(regel)) return` — die een hele regel oversloeg zodra er érgens een `t(` op stond — is vervangen door het herhaald wegknippen van `{…}`-waarden. ⚠️ **De echte reparatie is** `tests/scripts/tekst-controle.test.ts`: acht vormen die hij moet vinden en zes die hij met rust moet laten. Tegen de oude versie worden zeven van die acht rood. Dat is wat er ontbrak — de heuristieken waren niet slecht, ze zijn nooit tegen een bekend geval gelegd, want er wás geen manier om te zien wat de controle wél vindt zonder de codebase te wijzigen. 23 zinnen zijn daarna alsnog naar de catalogus verhuisd, waaronder twee `accessibilityLabel`s die een schermlezer voorleest. | ~~Middel~~ opgelost |
| 2026-08-24 | ✅ Oppervlak 1, 2 en 13 variëren nu wél op `groups.zichtbaarheid` | **Achterhaald dezelfde dag, en dat is zelf de bevinding.** Deze rij is geschreven toen alleen oppervlak 3 om was, en niet bijgewerkt toen migraties 0078 en 0079 diezelfde middag de andere drie omzetten — terwijl `CLAUDE.md` en `docs/WERKVOORRAAD.md` dat wél zeggen. Gevonden door de code-critic-ronde. ⚠️ **`npm run review:controle` kan dit niet vangen**: die toetst dát er een `Wordt zwaarder als:`-zin staat, niet of de rij nog waar is — precies het verschil tussen een eigenschap van het onderdeel en de belofte (regel 18). Wat er van de oorspronkelijke rij overblijft is de verzwakking bij oppervlak 2: de bescherming ging van "de kolom bestáát niet" naar "de kolom is leeg", en dat hangt van een `case` af in plaats van van een ontbrekende kolom. De afweging staat in de kop van 0078. | ~~Laag~~ opgelost |
| 2026-08-24 | 📏 `group_overview()` roept vier definer-functies aan per lid-rij | Gevonden door de code-critic-ronde op EPIC 13. Per rij: `shares_group_with_goal()` in de `where` van `group_visible_streaks`, `deelt_open_groep_met_doel()` **twee keer** (één per `case` in 0078) en `lid_van_open_groep()`. Alle vier zijn `security definer`, dus Postgres kan ze niet inlinen. Bij `p_limit` 50 zijn dat tot 200 executies waar het er vóór deze milestone 50 waren. `lid_van_open_groep(m.group_id)` krijgt bovendien voor élke rij hetzelfde argument, want `m.group_id = p_group_id` staat in de `where`. Onwrikbare regel 12 noemt groepsoverzichten met naam als de valkuil. ⚠️ **Niet gerepareerd**, en met opzet: dit is de heetste query van het project, de winst is niet gemeten, en een herschrijving met een CTE raakt precies de policy-laag die deze milestone net onder test heeft gebracht. Meten vóór sleutelen. **Wordt zwaarder als:** een groep boven de tien leden komt, of zodra het overzicht per periode gepagineerd wordt in plaats van per lid.  ✅ **Gemeten op 25-08-2026, en de diagnose hierboven klopte niet.** Lokaal een groep van 50 leden gebouwd (49 doelen, 1470 weekdoelen) en een van 200, en `group_overview()` gemeten met `explain (analyze, buffers)` als gewoon lid. Warm: **50 leden 4,6–6,9 ms** (546–722 buffers, vlak over `p_limit` 5/10/20/50), **200 leden 16,9–18,7 ms** (4328–4924 buffers, ook bij de laatste pagina). ⚠️ **De kosten schalen dus met de gróótte van de groep en niet met de paginagrootte** — en juist de vier definer-aanroepen zijn begrensd door `p_limit` (maximaal 50). Wat wél met elk lid meegroeit is alles vóór de `limit`: de `count(*) over ()` en de scan over `group_members`. De rij hierboven wees dus de verkeerde term aan. ⚠️ **Eén valkuil in het meten zelf, en die is het opschrijven waard:** de eerste meting gaf 45 ms en 17.614 buffers, en dat was een koude sessie — plancache en catalogus nog leeg. Warm is het een factor acht minder. Wie hier ooit opnieuw meet: gooi de eerste meting weg. **Nog steeds niet gerepareerd, en nu met een reden in plaats van een vermoeden:** 5 ms voor een buddy-groep (drie tot acht mensen) is niets, en een herschrijving raakt de policy-laag die deze milestone net onder test heeft gebracht. **Wordt zwaarder als:** een groep boven de honderd leden komt — daar is het gemeten 17 ms en groeit het lineair mee — of zodra het overzicht per periode gepagineerd wordt in plaats van per lid, want dan telt de `count(*) over ()` een ander geheel. | Laag |
| 2026-08-24 | Twee van de zeven "bewust dicht"-oppervlakken hebben geen test in de open stand | `tests/rls/epic13.test.ts` dekt punten, de ketting-teller, realtime en — sinds de reviewronde — de weekpassen. Wat ontbreekt is oppervlak 17, `approval_withdrawals`. Vandaag noemt die policy `zichtbaarheid` niet, dus er lekt niets; de reden is dat een zinnige test een volledige keten voltooiing → goedkeuring → intrekking in de fixture vraagt, en een test die op een lege tabel nul rijen telt bewijst niets. ⚠️ Beslisdocument 002 §6c zegt "wie er ooit een wil verruimen, komt langs deze tabel" — en een tabel is geen slot. **Wordt zwaarder als:** iemand aan `approval_withdrawals_select` komt, of zodra een achtste oppervlak bij de bewust-dichte lijst komt. | Laag |
| 2026-08-24 | ✅ Een bewaarde uitnodigingscode wordt zonder tweede vraag verzilverd, en verloopt nooit | Gevonden door de critical-user-ronde op EPIC 13. `Uitnodigingswacht` in `app/_layout.tsx` bewaart de code zodra iemand een uitnodigingslink opent — vóór hij iets gelezen heeft — en `neemDeel()` draait zodra de onboarding klaar is. Er is geen "nee, bedankt" op het uitnodigingsscherm en de code veroudert niet. ⚠️ **Sinds besluit A41 heeft toetreden dezelfde privacygevolgen als het ópenzetten**, waar een beheerder een volledig bevestigingsblok voor doorloopt. Dat is precies het patroon waar `CLAUDE.md` voor waarschuwt: een bestaande primitieve handeling waar een nieuw besluit op is gaan leunen zonder dat iemand hem opnieuw bekeken heeft. **Tweede helft — ✅ gerepareerd op 24-08:** mislukte `neemDeel()`, dan gebeurde er niets — geen melding — terwijl het uitnodigingsscherm belooft "ook als je eerst je e-mail moet bevestigen, sta je daarna gewoon in de groep". De gebruiker stond op een leeg dashboard zonder groep en zonder uitleg. De app stuurt hem nu naar `/uitnodiging/<code>`: dát scherm laadt de groep opnieuw, zegt wat er aan de hand is en heeft een knop om het opnieuw te proberen. ✅ **En de eerste helft is besloten en gebouwd op 24-08** — besluit **A49**, QS8-136, variant 2 én 3: een bewaarde code verloopt na 24 uur, en een **open** groep wordt nooit automatisch betreden. ⚠️ Een verlopen code wordt niet weggegooid maar getóónd; weggooien zou de uitnodiging doodmaken, en dat is precies wat deze opslag moest voorkomen. | ~~Middel~~ opgelost |
| 2026-08-24 | De tijdzonekeuze vindt alleen steden die een eigen IANA-zone hebben | Gemeten tegen `Intl`: "Rotterdam", "Manchester", "Osaka" en "Netherlands" geven nul treffers, want de lijst kent alleen zonenamen. Voor een Nederlandse gebruiker in de onboarding is dat een muur; "Geen tijdzone gevonden. Zoek op een grote stad in de buurt" wijst de goede kant op maar lost het niet op. ⚠️ Op een toestel zonder `Intl.supportedValuesOf` is de lijst zelfs leeg en werkt alleen de letterlijke zonenaam — precies de gebruiker die het veld het hardst nodig heeft. **Niet gerepareerd**: een plaatsnaam-naar-zone-tabel is data die onderhouden moet worden, en die keuze hoort bij het product en niet bij deze reviewronde. Wat wél gedaan is: de opgeslagen zone wordt genormaliseerd (`normaliseerZone()` in `shared/time`), zodat `europe/amsterdam` geen dubbele knop meer oplevert. **Wordt zwaarder als:** de app buiten Nederland gebruikt wordt, of zodra iemand de zone op tekst gaat vergelijken op een tweede plek. | Laag |
| 2026-08-25 | ✅ De Doelcoach was stuk in productie, en niets kon dat zien | `doelcoach/index.ts` las het profiel van de eigenaar met `await db`, maar de client heet in die functie `alsSysteem`. Geen typefout maar een `ReferenceError`: hij staat in de `try`, dus élke doelcoach-job viel in de `catch`, kreeg netjes `status = 'failed'` met `error = 'db is not defined'` en gaf HTTP 200 terug. De gedeployde versie 5 had hem ook — nagemeten met `get_edge_function`, niet aangenomen. Gevonden op de dag dat `supabase/functions/` voor het eerst getypecheckt werd; vóór die dag was er geen enkel mechanisme dat een niet-bestaande naam in deze map kon opmerken. ⚠️ **De vorm is opnieuw die van regel 18**: elk onderdeel klopte — de prompt, de taalinstructie, de kostenboeking, de claim-met-voorwaarde, allemaal getest — en de keten was dood. En hij was dubbel gecamoufleerd: de `catch` schreef een keurige mislukking weg en de functie antwoordde 200, precies zoals hij bedoeld was te doen bij een échte fout. **Repareren was één woord; het vinden vroeg een typechecker.** | ~~Hoog~~ opgelost |
| 2026-08-25 | 📏 De Edge-typecheck is niet lokaal te draaien zoals hij in CI staat | `jsr.io` en `npm.jsr.io` geven 403 op een agent-werkplek, dus `deno check` van de drie `index.ts`-bestanden kan hier niet zoals CI het doet. De omweg werkt wel en staat in de workflow: het jsr-pakket is dezelfde broncode als het npm-pakket, en `registry.npmjs.org` is wél bereikbaar — een kopie van de map met `npm:@supabase/supabase-js@<zelfde versie>` typecheckt volledig. Zo zijn de achttien fouten in `notificaties` én de `db`-fout in `doelcoach` hier gevonden in plaats van in drie CI-rondes. ⚠️ **Dit is een handeling die je moet onthouden en die niets kapotmaakt als je hem overslaat** — dezelfde vorm als het migratieregister vóór QS8-122, en die is toen een commando geworden. Hier is het dat nog niet: het vraagt een Deno op de werkplek, en die staat niet in `package.json`. **Wordt zwaarder als:** er een vierde Edge Function bijkomt, of zodra iemand hier een wijziging maakt die verder gaat dan één regel — dan is drie keer wachten op CI duurder dan het script schrijven. | Laag |
| 2026-08-25 | 📏 `points_ledger.reason` mag `goal_done` zijn en niemand boekt het ooit | Gevonden door de nieuwe ketencontrole, en de enige van vijf dode CHECK-waarden die een besluit vraagt in plaats van een epic. Nagemeten op de gedeployde database: geen enkele functie in `pg_proc` noemt `goal_done`, en `src/`, `app/` en `supabase/functions/` evenmin. De andere vier zijn onafgebouwde features (`majority`, `monthly`, `photo`, `doc`) en staan met hun voorwaarde in `BEWUST_ONGESCHREVEN`. ⚠️ **Waarom dit geen 0082 is.** Daar ging `missed` weg omdat niets hem schreef, punt. Hier is de vraag inhoudelijk: domeinregel 10 zegt dat het puntenplafond van een doel de sóm is van de plafondpunten van zijn weekdoelen — dan is een aparte boeking voor het afronden van het doel dubbeltelling, en hoort `goal_done` weg. Maar `milestone_done` staat er wél en wórdt geboekt, en die zit in diezelfde som. Zonder besluit schrappen verandert het puntenmodel, en dat is precies wat een migratie niet stilletjes hoort te doen. **Wordt zwaarder als:** er punten voor het afronden van een doel bijkomen zonder dat iemand het plafond opnieuw uitrekent, of zodra `rond_doel_af()` iets in `points_ledger` gaat schrijven. | Laag |
| 2026-08-25 | Een gearchiveerde groep is niet terug te lezen | Gevolg van 0092, bewust en met open eind. De archieftoets zit in `is_group_member()` omdat daar tien schrijfpolicies langskomen; `groups_select` loopt langs diezelfde functie, dus de chat, de weekafsluitingen en De Ketting van een gearchiveerde groep zijn voor niemand meer te openen. Er wordt niets gewist — de rijen staan er nog en de reeksen van de leden blijven kloppen — maar "archief" belooft leesbaarheid die er niet is, en de bevestigingstekst zegt dat daarom ook expliciet. ⚠️ **En er is geen weg terug:** na het archiveren geeft `is_group_admin()` onwaar voor die groep, dus een `heropen_groep()` kan die functie niet gebruiken om de beheerder te herkennen en moet rechtstreeks in `group_members` kijken. Dat staat in de kop van 0092 zodat de volgende het niet als een bug leest. **Wordt zwaarder als:** de eerste echte gebruiker een groep archiveert — vanaf dan is het verlies van leesbaarheid niet meer theoretisch — of zodra iemand een heropen-knop wil. | Laag |
